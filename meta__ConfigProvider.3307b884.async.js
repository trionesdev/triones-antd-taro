"use strict";(self.webpackChunk_trionesdev_antd_taro_react=self.webpackChunk_trionesdev_antd_taro_react||[]).push([[9895],{14497:function(t,e,n){var o,r;n.r(e),n.d(e,{demos:function(){return Ne}});var l=n(90819),p=n.n(l),m=n(89933),_=n.n(m),a=n(44194),i=n(55682),d=n(80955),s=n(49387),u=n(59063),f=n(497),v=n(9384),x=n(11211),g=n(30128),h=n(7574),P=n(33550),k=n(52456),E=n(42152),I=n(51434),C=n(99278),y=n(76373),R=n(53480),b=n(93977),M=n(81907),N=n(40526),w=n(52563),L=n(32482),O=n(73989),D=n(43068),T=n(37219),B=n(8391),S=n(54214),F=n(99038),A=n(8e3),$=n(24588),U=n(56639),W=n(16379),Z=n(50149),K=n(76461),V=n(70792),z=n(2936),H=n(3405),G=n(68846),j=n(70162),Y=n(56938),X=n(32697),J=n(92727),Q=n(84272),q=n(77153),nn=n(44164),en=n(51727),tn=n(63750),on=n(67485),rn=n(98958),an=n(93286),sn=n(37180),cn=n(12684),ln=n(17455),dn=n(42203),pn=n(15673),_n=n(69709),mn=n(24295),un=n(32061),fn=n(36758),vn=n(6281),xn=n(61272),gn=n(73079),hn=n(54222),Pn=n(19771),kn=n(70496),En=n(37173),In=n(31396),Cn=n(51865),Te=n.n(Cn),yn=n(12571),Rn=n(6769),bn=n(57858),Mn=n(69392),Ee=n(27311),Ie=n(81792),Nn=n(39378),Be=n.n(Nn),wn=n(67312),Ln=n(63346),Ce=n(56321),On=n(55888),Dn=n(95587),Tn=n(95611),Bn=n(69295),Sn=n(53974),Fn=n(90625),An=n(38664),$n=n(49442),Un=n(71348),Se=n.n(Un),Wn=n(39844),Zn=n(43427),ye=n(68161),Kn=n(71318),Vn=n(96858),zn=n(82926),Hn=n(13889),Gn=n(96380),jn=n(21497),Yn=n(23421),Xn=n(85279),Jn=n(43416),Qn=n(37575),qn=n(13299),ne=n(51332),ee=n(45867),Re=n(15272),te=n(63768),oe=n(95876),re=n(94667),be=n(81654),ae=n(37062),se=n(8418),ie=n(95432),ce=n(11421),le=n(61816),de=n(56694),pe=n(36607),_e=n(64989),me=n(10773),ue=n(3133),fe=n(82250),ve=n(95110),Me=n(7358),xe=n(74066),ge=n(13716),he=n(81060),Pe=n(10465),Ne={"src-config-provider-demo-base":{component:a.memo(a.lazy(function(){return Promise.all([n.e(9378),n.e(3849),n.e(1588),n.e(7311),n.e(1715),n.e(9985),n.e(9842),n.e(2433)]).then(n.bind(n,68925))})),asset:{type:"BLOCK",id:"src-config-provider-demo-base",refAtomIds:[],dependencies:{"index.tsx":{type:"FILE",value:n(71043).Z},react:{type:"NPM",value:"18.3.1"},"../../index.tsx":{type:"FILE",value:n(37687).Z},"../../DemoBlock.tsx":{type:"FILE",value:n(7761).Z},"../../locales/zh-CN.ts":{type:"FILE",value:n(87770).Z},"../../locales/en-US.ts":{type:"FILE",value:n(67198).Z},"../index.ts":{type:"FILE",value:n(56).Z},"./config-provider.tsx":{type:"FILE",value:n(32065).Z},"./base.ts":{type:"FILE",value:n(57982).Z},"../utils/merge-locale.ts":{type:"FILE",value:n(18001).Z},"./index.scss":{type:"FILE",value:n(55690).Z},"./Checkbox.tsx":{type:"FILE",value:n(29518).Z},"./style.ts":{type:"FILE",value:n(96334).Z},"./Avatar.tsx":{type:"FILE",value:n(77101).Z},"./PickerView.tsx":{type:"FILE",value:n(93452).Z},"./Badge.ts":{type:"FILE",value:n(29871).Z},"./Divider.ts":{type:"FILE",value:n(17651).Z},"./Button.tsx":{type:"FILE",value:n(77306).Z},"./Ellipsis.ts":{type:"FILE",value:n(63087).Z},"./Card.tsx":{type:"FILE",value:n(60383).Z},"./Empty.tsx":{type:"FILE",value:n(69820).Z},"./Radio.tsx":{type:"FILE",value:n(54007).Z},"./Rate.tsx":{type:"FILE",value:n(77635).Z},"./ErrorBlock.ts":{type:"FILE",value:n(69155).Z},"./Result.ts":{type:"FILE",value:n(83671).Z},"./Footer.ts":{type:"FILE",value:n(76183).Z},"./Space.ts":{type:"FILE",value:n(97297).Z},"./Grid.ts":{type:"FILE",value:n(29231).Z},"./Switch.ts":{type:"FILE",value:n(57151).Z},"./Images.ts":{type:"FILE",value:n(64528).Z},"./TabBar.tsx":{type:"FILE",value:n(31347).Z},"./Input.tsx":{type:"FILE",value:n(53531).Z},"./Tag.tsx":{type:"FILE",value:n(87990).Z},"./NavBar.ts":{type:"FILE",value:n(59914).Z},"./WaterMark.ts":{type:"FILE",value:n(46099).Z},"./NoticeBar.ts":{type:"FILE",value:n(62146).Z},"./Form.tsx":{type:"FILE",value:n(4157).Z},"./PageIndicator.ts":{type:"FILE",value:n(45326).Z},"./Popup.tsx":{type:"FILE",value:n(81891).Z},"./Popover.tsx":{type:"FILE",value:n(31223).Z},"./Picker.tsx":{type:"FILE",value:n(70431).Z},"./ProgressBar.tsx":{type:"FILE",value:n(8576).Z},"./checkbox.tsx":{type:"FILE",value:n(90179).Z},"./avatar.tsx":{type:"FILE",value:n(29003).Z},"./picker-view.tsx":{type:"FILE",value:n(40871).Z},"./style.scss":{type:"FILE",value:n(87223).Z},"./divider.tsx":{type:"FILE",value:n(71448).Z},"./ellipsis.tsx":{type:"FILE",value:n(81974).Z},"./result.tsx":{type:"FILE",value:n(44096).Z},"./rate.tsx":{type:"FILE",value:n(54898).Z},"./button.tsx":{type:"FILE",value:n(6458).Z},"./footer.tsx":{type:"FILE",value:n(97855).Z},"./space.tsx":{type:"FILE",value:n(55033).Z},"./switch.tsx":{type:"FILE",value:n(19635).Z},"./grid.tsx":{type:"FILE",value:n(48365).Z},"./nav-bar.tsx":{type:"FILE",value:n(80415).Z},"./tag.tsx":{type:"FILE",value:n(31257).Z},"./tab-bar.tsx":{type:"FILE",value:n(53336).Z},"./page-indicator.tsx":{type:"FILE",value:n(98188).Z},"./notice-bar.tsx":{type:"FILE",value:n(66130).Z},"./picker.tsx":{type:"FILE",value:n(23115).Z},"./popup.tsx":{type:"FILE",value:n(30226).Z},"./popover.tsx":{type:"FILE",value:n(93703).Z},"./progress-bar.tsx":{type:"FILE",value:n(46793).Z},"./water-mark.tsx":{type:"FILE",value:n(60053).Z},"./create-error-block.tsx":{type:"FILE",value:n(84282).Z},"./error-block.tsx":{type:"FILE",value:n(1130).Z},classnames:{type:"NPM",value:"2.5.1"},"./card.tsx":{type:"FILE",value:n(66370).Z},"./badge.tsx":{type:"FILE",value:n(37814).Z},"../utils/native-props.ts":{type:"FILE",value:n(12636).Z},"./image.tsx":{type:"FILE",value:n(76890).Z},"@trionesdev/antd-taro-icons-react":{type:"NPM",value:"0.0.1"},ahooks:{type:"NPM",value:"3.8.4"},lodash:{type:"NPM",value:"4.17.21"},"./empty.tsx":{type:"FILE",value:n(85871).Z},"./input-opt.tsx":{type:"FILE",value:n(21658).Z},"react-dom":{type:"NPM",value:"18.3.1"},"./input-password.tsx":{type:"FILE",value:n(55713).Z},"./input.tsx":{type:"FILE",value:n(71537).Z},"./hooks/useForm.ts":{type:"FILE",value:n(22509).Z},"./form.tsx":{type:"FILE",value:n(14230).Z},"./FormItem.tsx":{type:"FILE",value:n(18437).Z},"../utils/attach-properties-to-component.ts":{type:"FILE",value:n(28745).Z},"./group.tsx":{type:"FILE",value:n(8286).Z},"./radio.tsx":{type:"FILE",value:n(33591).Z},"staged-components":{type:"NPM",value:"1.1.3"},"./images.tsx":{type:"FILE",value:n(67928).Z},"./popup-modal.tsx":{type:"FILE",value:n(67353).Z},"rc-field-form":{type:"NPM",value:"2.7.0"},"./form-item.tsx":{type:"FILE",value:n(21106).Z},"../utils/with-default-props.tsx":{type:"FILE",value:n(62055).Z},"./native-props.tsx":{type:"FILE",value:n(1368).Z},"./picker-view-column.tsx":{type:"FILE",value:n(26585).Z},"./use-result-icon.tsx":{type:"FILE",value:n(87621).Z},"./input-affix-wrapper.tsx":{type:"FILE",value:n(51060).Z},"./context.tsx":{type:"FILE",value:n(98229).Z},"./base-input.tsx":{type:"FILE",value:n(7738).Z},"./useMeasure.tsx":{type:"FILE",value:n(79299).Z},"../utils/with-stop-propagation.tsx":{type:"FILE",value:n(85898).Z},"../utils/use-resize-effect.tsx":{type:"FILE",value:n(4684).Z},"./group-context.tsx":{type:"FILE",value:n(15311).Z},"../utils/use-props-value.ts":{type:"FILE",value:n(21629).Z},"./busy.tsx":{type:"FILE",value:n(4009).Z},"./default.tsx":{type:"FILE",value:n(79905).Z},"./disconnected.tsx":{type:"FILE",value:n(17779).Z},"rc-util":{type:"NPM",value:"5.44.3"},"./spin-icon.tsx":{type:"FILE",value:n(14452).Z},"../utils/validate.ts":{type:"FILE",value:n(32792).Z},"../hooks/useFormContext.tsx":{type:"FILE",value:n(13102).Z},"./form-item-label.tsx":{type:"FILE",value:n(91438).Z},"./form-item-input.tsx":{type:"FILE",value:n(82159).Z},"../utils/to-css-length.ts":{type:"FILE",value:n(84601).Z},"../utils/use-isomorphic-update-layout-effect.ts":{type:"FILE",value:n(91857).Z},"./broken-image-icon.tsx":{type:"FILE",value:n(83581).Z},"./image-icon.tsx":{type:"FILE",value:n(42080).Z},"./lazy-detector.tsx":{type:"FILE",value:n(76172).Z},"../utils/use-mutation-effect.tsx":{type:"FILE",value:n(35969).Z},"./picker-view-column-item.tsx":{type:"FILE",value:n(59105).Z},runes2:{type:"NPM",value:"1.1.4"},"../utils/is-dev.ts":{type:"FILE",value:n(51630).Z},"../Icon/check-icon.tsx":{type:"FILE",value:n(87122).Z},"../Input/native-input.tsx":{type:"FILE",value:n(69751).Z},"../utils/dev-log.ts":{type:"FILE",value:n(77215).Z}},entry:"index.tsx"},context:{"../../index.tsx":d,"../../DemoBlock.tsx":s,"../../locales/zh-CN.ts":u,"../../locales/en-US.ts":f,"../index.ts":v,"./config-provider.tsx":x,"./base.ts":g,"../utils/merge-locale.ts":h,"./index.scss":P,"./Checkbox.tsx":k,"./style.ts":E,"./Avatar.tsx":I,"./PickerView.tsx":C,"./Badge.ts":y,"./Divider.ts":R,"./Button.tsx":b,"./Ellipsis.ts":M,"./Card.tsx":N,"./Empty.tsx":w,"./Radio.tsx":L,"./Rate.tsx":O,"./ErrorBlock.ts":D,"./Result.ts":T,"./Footer.ts":B,"./Space.ts":S,"./Grid.ts":F,"./Switch.ts":A,"./Images.ts":$,"./TabBar.tsx":U,"./Input.tsx":W,"./Tag.tsx":Z,"./NavBar.ts":K,"./WaterMark.ts":V,"./NoticeBar.ts":z,"./Form.tsx":H,"./PageIndicator.ts":G,"./Popup.tsx":j,"./Popover.tsx":Y,"./Picker.tsx":X,"./ProgressBar.tsx":J,"./checkbox.tsx":Q,"./avatar.tsx":q,"./picker-view.tsx":nn,"./style.scss":en,"./divider.tsx":tn,"./ellipsis.tsx":on,"./result.tsx":rn,"./rate.tsx":an,"./button.tsx":sn,"./footer.tsx":cn,"./space.tsx":ln,"./switch.tsx":dn,"./grid.tsx":pn,"./nav-bar.tsx":_n,"./tag.tsx":mn,"./tab-bar.tsx":un,"./page-indicator.tsx":fn,"./notice-bar.tsx":vn,"./picker.tsx":xn,"./popup.tsx":gn,"./popover.tsx":hn,"./progress-bar.tsx":Pn,"./water-mark.tsx":kn,"./create-error-block.tsx":En,"./error-block.tsx":In,"./card.tsx":yn,"./badge.tsx":Rn,"../utils/native-props.ts":bn,"./image.tsx":Mn,"./empty.tsx":wn,"./input-opt.tsx":Ln,"./input-password.tsx":On,"./input.tsx":Dn,"./hooks/useForm.ts":Tn,"./form.tsx":Bn,"./FormItem.tsx":Sn,"../utils/attach-properties-to-component.ts":Fn,"./group.tsx":An,"./radio.tsx":$n,"./images.tsx":Wn,"./popup-modal.tsx":Zn,"./form-item.tsx":Kn,"../utils/with-default-props.tsx":Vn,"./native-props.tsx":zn,"./picker-view-column.tsx":Hn,"./use-result-icon.tsx":Gn,"./input-affix-wrapper.tsx":jn,"./context.tsx":Yn,"./base-input.tsx":Xn,"./useMeasure.tsx":Jn,"../utils/with-stop-propagation.tsx":Qn,"../utils/use-resize-effect.tsx":qn,"./group-context.tsx":ne,"../utils/use-props-value.ts":ee,"./busy.tsx":te,"./default.tsx":oe,"./disconnected.tsx":re,"./spin-icon.tsx":ae,"../utils/validate.ts":se,"../hooks/useFormContext.tsx":ie,"./form-item-label.tsx":ce,"./form-item-input.tsx":le,"../utils/to-css-length.ts":de,"../utils/use-isomorphic-update-layout-effect.ts":pe,"./broken-image-icon.tsx":_e,"./image-icon.tsx":me,"./lazy-detector.tsx":ue,"../utils/use-mutation-effect.tsx":fe,"./picker-view-column-item.tsx":ve,"../utils/is-dev.ts":xe,"../Icon/check-icon.tsx":ge,"../Input/native-input.tsx":he,"../utils/dev-log.ts":Pe,react:o||(o=n.t(a,2)),"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/index.tsx":d,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/DemoBlock/index.tsx":s,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/locales/zh-CN.ts":u,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/locales/en-US.ts":f,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ConfigProvider/index.ts":v,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ConfigProvider/config-provider.tsx":x,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/locales/base.ts":g,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/merge-locale.ts":h,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/index.scss":P,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Checkbox/index.tsx":k,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/style/index.ts":E,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Avatar/index.tsx":I,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/PickerView/index.tsx":C,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Badge/index.ts":y,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Divider/index.ts":R,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Button/index.tsx":b,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Ellipsis/index.ts":M,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Card/index.tsx":N,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Empty/index.tsx":w,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Radio/index.tsx":L,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Rate/index.tsx":O,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/index.ts":D,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Result/index.ts":T,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Footer/index.ts":B,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Space/index.ts":S,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Grid/index.ts":F,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Switch/index.ts":A,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Images/index.ts":$,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/TabBar/index.tsx":U,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/index.tsx":W,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Tag/index.tsx":Z,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/NavBar/index.ts":K,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/WaterMark/index.ts":V,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/NoticeBar/index.ts":z,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/index.tsx":H,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/PageIndicator/index.ts":G,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Popup/index.tsx":j,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Popover/index.tsx":Y,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Picker/index.tsx":X,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ProgressBar/index.tsx":J,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Checkbox/checkbox.tsx":Q,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Avatar/avatar.tsx":q,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/PickerView/picker-view.tsx":nn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/NoticeBar/style.scss":en,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Divider/divider.tsx":tn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Ellipsis/ellipsis.tsx":on,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Result/result.tsx":rn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Rate/rate.tsx":an,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Button/button.tsx":sn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Footer/footer.tsx":cn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Space/space.tsx":ln,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Switch/switch.tsx":dn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Grid/grid.tsx":pn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/NavBar/nav-bar.tsx":_n,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Tag/tag.tsx":mn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/TabBar/tab-bar.tsx":un,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/PageIndicator/page-indicator.tsx":fn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/NoticeBar/notice-bar.tsx":vn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Picker/picker.tsx":xn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Popup/popup.tsx":gn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Popover/popover.tsx":hn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ProgressBar/progress-bar.tsx":Pn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/WaterMark/water-mark.tsx":kn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/create-error-block.tsx":En,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/error-block.tsx":In,classnames:Cn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Card/card.tsx":yn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Badge/badge.tsx":Rn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/native-props.ts":bn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Images/image.tsx":Mn,"@trionesdev/antd-taro-icons-react":Ee,ahooks:Ie,lodash:Nn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/images/empty.tsx":wn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/input-opt.tsx":Ln,"react-dom":r||(r=n.t(Ce,2)),"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/input-password.tsx":On,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/input.tsx":Dn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/hooks/useForm.ts":Tn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/form.tsx":Bn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/FormItem/index.tsx":Sn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/attach-properties-to-component.ts":Fn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Radio/group.tsx":An,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Radio/radio.tsx":$n,"staged-components":Un,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/images/index.tsx":Wn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Popup/popup-modal.tsx":Zn,"rc-field-form":ye,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/FormItem/form-item.tsx":Kn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/with-default-props.tsx":Vn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Card/native-props.tsx":zn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/PickerView/picker-view-column.tsx":Hn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Result/use-result-icon.tsx":Gn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/input-affix-wrapper.tsx":jn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/context.tsx":Yn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/base-input.tsx":Xn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Ellipsis/useMeasure.tsx":Jn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/with-stop-propagation.tsx":Qn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/use-resize-effect.tsx":qn,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Radio/group-context.tsx":ne,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/use-props-value.ts":ee,"@trionesdev/antd-taro-icons-react/src":Re,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/images/busy.tsx":te,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/images/default.tsx":oe,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/ErrorBlock/images/disconnected.tsx":re,"rc-util":be,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Switch/spin-icon.tsx":ae,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/validate.ts":se,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/hooks/useFormContext.tsx":ie,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/FormItem/form-item-label.tsx":ce,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Form/FormItem/form-item-input.tsx":le,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/to-css-length.ts":de,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/use-isomorphic-update-layout-effect.ts":pe,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Images/broken-image-icon.tsx":_e,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Images/image-icon.tsx":me,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Images/lazy-detector.tsx":ue,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/use-mutation-effect.tsx":fe,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/PickerView/picker-view-column-item.tsx":ve,runes2:Me,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/is-dev.ts":xe,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Icon/check-icon.tsx":ge,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/Input/native-input.tsx":he,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-taro-react/src/utils/dev-log.ts":Pe},renderOpts:{compile:function(){var we=_()(p()().mark(function Oe(){var ke,De=arguments;return p()().wrap(function(c){for(;;)switch(c.prev=c.next){case 0:return c.next=2,n.e(4521).then(n.bind(n,24521));case 2:return c.abrupt("return",(ke=c.sent).default.apply(ke,De));case 3:case"end":return c.stop()}},Oe)}));function Le(){return we.apply(this,arguments)}return Le}()}}}},30128:function(t,e,n){n.r(e),n.d(e,{base:function(){return r}});var o="${label} is not a valid ${type}",r={locale:"en",common:{confirm:"Confirm",cancel:"Cancel",loading:"Loading",close:"Close"},Calendar:{title:"Date selection",confirm:"Confirm",start:"start",end:"end",today:"today",markItems:["Mon","Tue","Wed","Thu","Fri","Sat","Sun"],yearAndMonth:"${year}/${month}"},Cascader:{placeholder:"Selecting"},Dialog:{ok:"OK"},DatePicker:{tillNow:"Till Now"},ErrorBlock:{default:{title:"Oops, something went wrong",description:"Please wait a minute and try again"},busy:{title:"Oops, not loading",description:"Try to refresh the page"},disconnected:{title:"Network is busy",description:"Try to refresh the page"},empty:{title:"Hmm, couldn't find that...",description:"Want to try a new search?"}},Form:{required:"Required",optional:"Optional",defaultValidateMessages:{default:"Field validation error for ${label}",required:"Please enter ${label}",enum:"${label} must be one of [${enum}]",whitespace:"${label} cannot be a blank character",date:{format:"${label} date format is invalid",parse:"${label} cannot be converted to a date",invalid:"${label} is an invalid date"},types:{string:o,method:o,array:o,object:o,number:o,date:o,boolean:o,integer:o,float:o,regexp:o,email:o,url:o,hex:o},string:{len:"${label} must be ${len} characters",min:"${label} must be at least ${min} characters",max:"${label} must be up to ${max} characters",range:"${label} must be between ${min}-${max} characters"},number:{len:"${label} must be equal to ${len}",min:"${label} must be minimum ${min}",max:"${label} must be maximum ${max}",range:"${label} must be between ${min}-${max}"},array:{len:"Must be ${len} ${label}",min:"At least ${min} ${label}",max:"At most ${max} ${label}",range:"The amount of ${label} must be between ${min}-${max}"},pattern:{mismatch:"${label} does not match the pattern ${pattern}"}}},ImageUploader:{uploading:"Uploading...",upload:"Upload"},InfiniteScroll:{noMore:"No more",failedToLoad:"Failed to load",retry:"Retry"},Input:{clear:"clear"},Mask:{name:"Mask"},Modal:{ok:"OK"},PasscodeInput:{name:"Passcode Input"},PullToRefresh:{pulling:"Scroll down to refresh",canRelease:"Release to refresh immediately",complete:"Refresh successful"},SearchBar:{name:"Search Bar"},Slider:{name:"Slider"},Stepper:{decrease:"decrease",increase:"increase"},Switch:{name:"Switch"},Selector:{name:"Selector"}}},497:function(t,e,n){n.r(e);var o=n(7574),r=n(30128),l=(0,o.mergeLocale)(r.base,{});e.default=l},7574:function(t,e,n){n.r(e),n.d(e,{mergeLocale:function(){return l}});var o=n(89957),r=n.n(o);function l(p,m){function _(a,i){if(r()(a)!=="object"||r()(i)!=="object"||Array.isArray(a)||Array.isArray(i))return i!==void 0?i:a;var d={};for(var s in a)a.hasOwnProperty(s)&&(d[s]=_(a[s],i[s]));return d}return _(p,m)}},67715:function(t,e,n){n.r(e),n.d(e,{texts:function(){return r}});var o=n(55682);const r=[{value:"\u7528\u4E8E\u5168\u5C40\u914D\u7F6E\u672C\u5730\u5316\u6587\u6848\u548C\u4E2A\u6027\u5316\u56FE\u6807\u3002",paraId:0,tocIndex:0},{value:"\u60A8\u60F3\u4F7F\u7528\u9664\u82F1\u6587\u4E4B\u5916\u7684\u8BED\u8A00\u3002",paraId:1,tocIndex:1},{value:"\u60A8\u60F3\u4F7F\u7528\u81EA\u5DF1\u7684\u56FE\u6807\u96C6\uFF0C\u800C\u4E0D\u662F\u5185\u7F6E\u7684\u56FE\u6807\u96C6\u3002",paraId:1,tocIndex:1},{value:"\u5C5E\u6027",paraId:2,tocIndex:4},{value:"\u8BF4\u660E",paraId:2,tocIndex:4},{value:"\u7C7B\u578B",paraId:2,tocIndex:4},{value:"\u9ED8\u8BA4\u503C",paraId:2,tocIndex:4},{value:"locale",paraId:2,tocIndex:4},{value:"\u672C\u5730\u5316\u6587\u6848",paraId:2,tocIndex:4},{value:"Locale",paraId:2,tocIndex:4},{value:"[zh-CN]",paraId:2,tocIndex:4},{value:"checkList",paraId:2,tocIndex:4},{value:"CheckList \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ activeIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"collapse",paraId:2,tocIndex:4},{value:"Collapse \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ arrowIcon?: ReactNode | ((active: boolean) => ReactNode) }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"dropdown",paraId:2,tocIndex:4},{value:"Dropdown \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ arrowIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"form",paraId:2,tocIndex:4},{value:"Form \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ helpIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"input",paraId:2,tocIndex:4},{value:"Input \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ clearIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"list",paraId:2,tocIndex:4},{value:"List \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ arrowIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"navBar",paraId:2,tocIndex:4},{value:"NavBar \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ backIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"noticeBar",paraId:2,tocIndex:4},{value:"NoticeBar \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ icon?: ReactNode, closeIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"popup",paraId:2,tocIndex:4},{value:"Popup \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ closeIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"result",paraId:2,tocIndex:4},{value:"Result \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ successIcon?: ReactNode, errorIcon?: ReactNode, infoIcon?: ReactNode, waitingIcon?: ReactNode, warningIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"searchBar",paraId:2,tocIndex:4},{value:"SearchBar \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ searchIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4},{value:"toast",paraId:2,tocIndex:4},{value:"Toast \u914D\u7F6E",paraId:2,tocIndex:4},{value:"{ successIcon?: ReactNode, failIcon?: ReactNode, loadingIcon?: ReactNode }",paraId:2,tocIndex:4},{value:"-",paraId:2,tocIndex:4}]},29003:function(t,e){e.Z=`import classNames from 'classnames';
import React from 'react';
import './style.scss';

type AvatarProps = {
  alt?: string;
  icon?: React.ReactNode;
  shape?: 'circle' | 'square';
  size?: number;
  src?: string | React.ReactNode;
  srcSet?: string;
  style?: React.CSSProperties;
  children?: React.ReactNode;
};

export const Avatar: React.FC<AvatarProps> = ({
  alt,
  icon,
  shape = 'circle',
  size = 40,
  src,
  srcSet,
  style,
  children,
}) => {
  const clsPrefix = 'triones-antm-avatar';
  return (
    <div
      className={classNames(clsPrefix, {
        [\`\${clsPrefix}-\${shape !== 'square' ? 'circle' : 'square'}\`]: true,
      })}
      style={{
        width: size,
        height: size,
        backgroundColor: src ? undefined : '#ccc',
        ...style,
      }}
    >
      {src ? (
        typeof src === 'string' ? (
          <img
            src={src}
            srcSet={srcSet}
            alt={alt}
            style={{
              width: '100%',
              height: '100%',
              objectFit: 'cover',
            }}
          />
        ) : (
          src
        )
      ) : icon ? (
        icon
      ) : (
        children
      )}
    </div>
  );
};

export default Avatar;
`},77101:function(t,e){e.Z=`import { Avatar } from "./avatar"

export default Avatar `},37814:function(t,e){e.Z=`import classNames from 'classnames'
import React from 'react'
import type { FC, ReactNode, CSSProperties } from 'react'
import { NativeProps, withNativeProps } from '../utils/native-props'

import './style.scss'

const classPrefix = \`triones-antm-badge\`

export const dot = <React.Fragment />

export type BadgeProps = {
  content?: ReactNode | typeof dot
  color?: string
  bordered?: boolean
  children?: ReactNode
  wrapperClassName?: string
  wrapperStyle?: CSSProperties
} & NativeProps<'--right' | '--top' | '--color'>

export const Badge: FC<BadgeProps> = props => {
  const { content, color, children } = props

  const isDot = content === dot

  const badgeClass = classNames(classPrefix, {
    [\`\${classPrefix}-fixed\`]: !!children,
    [\`\${classPrefix}-dot\`]: isDot,
    [\`\${classPrefix}-bordered\`]: props.bordered,
  })

  const element =
    content || content === 0
      ? withNativeProps(
        props,
        <div
          className={badgeClass}
          style={
            {
              '--color': color,
            } as BadgeProps['style']
          }
        >
          {!isDot && (
            <div className={\`\${classPrefix}-content\`}>{content}</div>
          )}
        </div>
      )
      : null

  return children ? (
    <div
      className={classNames(\`\${classPrefix}-wrapper\`, props.wrapperClassName)}
      style={props.wrapperStyle}
    >
      {children}
      {element}
    </div>
  ) : (
    element
  )
}
`},29871:function(t,e){e.Z=`import { attachPropertiesToComponent } from '../utils/native-props'
import { Badge, dot } from './badge'
export type { BadgeProps } from './badge'

export default attachPropertiesToComponent(Badge, { dot })
`},66370:function(t,e){e.Z=`import classNames from 'classnames';
import type { CSSProperties, FC, ReactNode } from 'react';
import React from 'react';
import { NativeProps, withNativeProps } from './native-props';

const classPrefix = \`triones-antm-card\`;

export type CardProps = {
  title?: ReactNode;
  icon?: ReactNode;
  extra?: ReactNode;
  headerStyle?: CSSProperties;
  headerClassName?: string;
  bodyStyle?: CSSProperties;
  bodyClassName?: string;
  onClick?: (event: React.MouseEvent<HTMLDivElement, MouseEvent>) => void;
  onBodyClick?: (event: React.MouseEvent<HTMLDivElement, MouseEvent>) => void;
  onHeaderClick?: (event: React.MouseEvent<HTMLDivElement, MouseEvent>) => void;
  children?: ReactNode;
} & NativeProps;

export const Card: FC<CardProps> = (props) => {
  const renderHeader = () => {
    if (!(props.title || props.extra)) {
      return null;
    }
    return (
      <div
        className={classNames(\`\${classPrefix}-header\`, props.headerClassName)}
        style={props.headerStyle}
        onClick={props.onHeaderClick}
      >
        {props.icon && (
          <div className={\`\${classPrefix}-header-icon\`}>{props.icon}</div>
        )}
        <div className={\`\${classPrefix}-header-title\`}>{props.title}</div>
        {props.extra && (
          <div className={\`\${classPrefix}-header-extra\`}>{props.extra}</div>
        )}
      </div>
    );
  };

  const renderBody = () => {
    if (!props.children) {
      return null;
    }
    return (
      <div
        className={classNames(\`\${classPrefix}-body\`, props.bodyClassName)}
        style={props.bodyStyle}
        onClick={props.onBodyClick}
      >
        {props.children}
      </div>
    );
  };

  return withNativeProps(
    props,
    <div className={classPrefix} onClick={props.onClick}>
      {renderHeader()}
      {renderBody()}
    </div>,
  );
};
`},60383:function(t,e){e.Z=`import { Card } from './card';
import './style.scss';
export type { CardProps } from './card';

export default Card;
`},1368:function(t,e){e.Z=`import classNames from 'classnames';
import type { CSSProperties, ReactElement } from 'react';
import React, { AriaAttributes } from 'react';

export type NativeProps<S extends string = never> = {
  className?: string;
  style?: CSSProperties & Partial<Record<S, string>>;
  tabIndex?: number;
} & AriaAttributes;

export function withNativeProps<P extends NativeProps>(
  props: P,
  element: ReactElement,
) {
  const p = {
    ...element.props,
  };
  if (props.className) {
    p.className = classNames(element.props.className, props.className);
  }
  if (props.style) {
    p.style = {
      ...p.style,
      ...props.style,
    };
  }
  if (props.tabIndex !== undefined) {
    p.tabIndex = props.tabIndex;
  }
  for (const key in props) {
    if (!props.hasOwnProperty(key)) continue;
    if (key.startsWith('data-') || key.startsWith('aria-')) {
      p[key] = props[key];
    }
  }
  return React.cloneElement(element, p);
}
`},90179:function(t,e){e.Z=`import "./index.scss"
import classNames from "classnames";
import React, {FC, useState} from "react";
import {CheckOutline} from "@trionesdev/antd-taro-icons-react";

type CheckboxProps = {
  children?: React.ReactNode;
  checked?: boolean;
  shape?: 'button' | 'round'
  disabled?: boolean
}
export const Checkbox: FC<CheckboxProps> = ({children, checked, shape = 'round', disabled}) => {
  const [innerChecked, setInnerChecked] = useState(checked)
  const cls = 'triones-antm-checkbox-item';
  return <label className={classNames(cls)}>
    <div className={classNames(\`\${cls}-container\`)} style={{pointerEvents: disabled ? 'none' : 'auto'}}>
      {shape === 'round' && <>
        <div className={classNames(\`\${cls}-wrap\`)}>
          <span className={classNames(\`\${cls}-base\`)}>
            <input className={classNames(\`\${cls}-input\`)} type={"checkbox"}
                   onChange={(e: React.ChangeEvent<HTMLInputElement>) => {
                     setInnerChecked(e.target.checked)
                   }}/>
          </span>

          <div className={classNames(\`\${cls}-fake\`)}>
            <CheckOutline className={classNames(\`\${cls}-fake-icon\`, {
              'checkedIcon': !disabled && innerChecked,
              'uncheckedIcon': !disabled && !innerChecked,
              'disabledCheckedIcon': disabled && innerChecked,
              'disabledUncheckedIcon': disabled && !innerChecked,
            })}/>
          </div>
        </div>
        <div className={classNames(\`\${cls}-content\`, {[\`\${cls}-content-disabled\`]: disabled})}>{children}</div>
      </>}
      {shape === 'button' && <div className={classNames(\`\${cls}-button\`, {
        [\`\${cls}-button-disabled\`]: disabled,
        [\`\${cls}-button-checked\`]: innerChecked
      })} onClick={() => {
        setInnerChecked(!innerChecked)
      }}>
        <div className={classNames(\`\${cls}-button-content\`)}>{children}</div>
        {innerChecked && <div className={classNames(\`\${cls}-button-icon\`)}>
          <CheckOutline className={classNames(\`checkedIcon\`)}/>
        </div>}
      </div>}
    </div>
  </label>
}
`},29518:function(t,e){e.Z=`import {Checkbox} from "./checkbox"

export default Checkbox
`},71043:function(t,e){e.Z=`import React from 'react'
import { DemoBlock } from '../../DemoBlock';
import zhCN from '../../locales/zh-CN'
import enUS from '../../locales/en-US'
import ConfigProvider from "../index";
import {ErrorBlock} from "../../index";

export default () => {
  return (
    <>
      <DemoBlock title='\u4E2D\u6587'>
        <ConfigProvider locale={zhCN}>
          <ErrorBlock status='disconnected' />
        </ConfigProvider>
      </DemoBlock>

      <DemoBlock title='\u82F1\u6587'>
        <ConfigProvider locale={enUS}>
          <ErrorBlock status='disconnected' />
        </ConfigProvider>
      </DemoBlock>
    </>
  )
}
`},71448:function(t,e){e.Z=`import React, { FC } from 'react'
import classNames from 'classnames'
import type { CSSProperties, ReactNode } from 'react'
import './style.scss'

interface BasicComponent {
  className?: string
  style?: CSSProperties
  children?: ReactNode
  id?: string
}

export type DividerContentPosition = 'left' | 'center' | 'right'
export type DividerDirection = 'horizontal' | 'vertical'

export interface DividerProps extends BasicComponent {
  contentPosition: DividerContentPosition
  direction?: DividerDirection
}

const ComponentDefaults = {
  className: '',
  style: {},
}

const defaultProps = {
  ...ComponentDefaults,
  contentPosition: 'center',
  direction: 'horizontal',
} as DividerProps

const classPrefix = \`triones-antm-divider\`

export const Divider: FC<Partial<DividerProps> & React.HTMLAttributes<HTMLDivElement>> = (props) => {
  const { children, contentPosition, style, className, direction, ...rest } = {
    ...defaultProps,
    ...props,
  }
  const classes =
    direction === 'horizontal'
      ? classNames({
        [\`\${classPrefix}\`]: true,
        [\`\${classPrefix}-center\`]: children,
        [\`\${classPrefix}-left\`]: contentPosition === 'left',
        [\`\${classPrefix}-right\`]: contentPosition === 'right',
        [\`\${classPrefix}-hairline\`]: true,
      })
      : classNames({
        [\`\${classPrefix}\`]: true,
        [\`\${classPrefix}-vertical\`]: direction === 'vertical',
      })
  return (
    <div className={\`\${classes} \${className || ''}\`} style={style} {...rest}>
      {children}
    </div>
  )
}
`},17651:function(t,e){e.Z=`import { Divider } from './divider'

export default Divider
`},81974:function(t,e){e.Z=`
import type { FC, ReactNode } from 'react'
import React from 'react'
import useMeasure from './useMeasure'
import { NativeProps, withNativeProps } from '../utils/native-props'
import { useResizeEffect } from '../utils/use-resize-effect'
import { PropagationEvent, withStopPropagation } from '../utils/with-stop-propagation'

import './style.scss'
import { mergeProps } from '../utils/with-default-props'

const classPrefix = \`triones-antm-ellipsis\`

export type EllipsisProps = {
  content: string
  direction?: 'start' | 'end' | 'middle'
  rows?: number
  expandText?: ReactNode
  collapseText?: ReactNode
  stopPropagationForActionButtons?: PropagationEvent[]
  onContentClick?: (e: React.MouseEvent<HTMLDivElement, MouseEvent>) => void
  defaultExpanded?: boolean
} & NativeProps

const defaultProps = {
  direction: 'end' as const,
  rows: 1,
  expandText: '',
  content: '',
  collapseText: '',
  stopPropagationForActionButtons: [],
  onContentClick: () => { },
  defaultExpanded: false,
}

export const Ellipsis: FC<EllipsisProps> = p => {
  const props = mergeProps(defaultProps, p) as EllipsisProps

  const {
    content,
    direction = 'end',
    rows = 1,
    expandText,
    collapseText,
    stopPropagationForActionButtons = [],
    onContentClick = () => { },
    defaultExpanded = false,
  } = props

  // ============================ Refs ============================
  const rootRef = React.useRef<HTMLDivElement>(null)

  // ========================== Expanded ==========================
  const [expanded, setExpanded] = React.useState<boolean>(defaultExpanded)

  const expandNode = expandText
    ? withStopPropagation(
      stopPropagationForActionButtons,
      <a
        onClick={() => {
          setExpanded(true)
        }}
      >
        {expandText}
      </a>
    )
    : null

  const collapseNode = collapseText
    ? withStopPropagation(
      stopPropagationForActionButtons,
      <a
        onClick={() => {
          setExpanded(false)
        }}
      >
        {collapseText}
      </a>
    )
    : null

  // ========================== Ellipsis ==========================
  const [measureNodes, forceResize] = useMeasure(
    rootRef,
    content,
    rows,
    direction,
    expanded,
    expandNode,
    collapseNode
  )

  useResizeEffect(forceResize, rootRef)

  // =========================== Render ===========================
  return withNativeProps(
    props,
    <div
      ref={rootRef}
      className={classPrefix}
      onClick={e => {
        if (e.target === e.currentTarget) {
          onContentClick(e)
        }
      }}
    >
      {measureNodes}
    </div>
  )
}
`},63087:function(t,e){e.Z=`import { Ellipsis } from './ellipsis'

export type { EllipsisProps } from './ellipsis'

export default Ellipsis
`},79299:function(t,e){e.Z=`import { useEvent } from 'rc-util'
import React from 'react'
import runes from 'runes2'

const enum MEASURE_STATUS {
  PREPARE = 1,
  MEASURE_WALKING = 2,
  STABLE_ELLIPSIS = 99,
  STABLE_NO_ELLIPSIS = 100,
}

const ELLIPSIS_TEXT = '...'

const measureStyle: React.CSSProperties = {
  visibility: 'hidden',
  whiteSpace: 'inherit',
  lineHeight: 'inherit',
  fontSize: 'inherit',
}

export default function useMeasure(
  containerRef: React.RefObject<HTMLDivElement>,
  content: string,
  rows: number,
  direction: 'start' | 'end' | 'middle',
  expanded: boolean,
  expandNode: React.ReactElement | null,
  collapseNode: React.ReactElement | null
) {
  const contentChars = React.useMemo(() => runes(content), [content])

  const [maxHeight, setMaxHeight] = React.useState<number>(0)

  const [walkingIndexes, setWalkingIndexes] = React.useState<
    [startOffset: number, endOffset: number]
  >([0, 0])
  const midIndex = Math.ceil((walkingIndexes[0] + walkingIndexes[1]) / 2)

  const [status, setStatus] = React.useState<MEASURE_STATUS>(
    MEASURE_STATUS.STABLE_NO_ELLIPSIS
  )

  // ============================ Refs ============================
  const singleRowMeasureRef = React.useRef<HTMLDivElement>(null)
  const fullMeasureRef = React.useRef<HTMLDivElement>(null)
  const midMeasureRef = React.useRef<HTMLDivElement>(null)

  const startMeasure = useEvent(() => {
    setStatus(MEASURE_STATUS.PREPARE)
    setWalkingIndexes([
      0,
      direction === 'middle'
        ? Math.ceil(contentChars.length / 2)
        : contentChars.length,
    ])
  })

  // Initialize
  React.useLayoutEffect(() => {
    startMeasure()
  }, [contentChars, rows])

  // Measure element height
  React.useLayoutEffect(() => {
    if (status === MEASURE_STATUS.PREPARE) {
      const fullMeasureHeight = fullMeasureRef.current?.offsetHeight || 0
      const singleRowMeasureHeight =
        singleRowMeasureRef.current?.offsetHeight || 0
      const rowMeasureHeight = singleRowMeasureHeight * rows

      if (fullMeasureHeight <= rowMeasureHeight) {
        setStatus(MEASURE_STATUS.STABLE_NO_ELLIPSIS)
      } else {
        setMaxHeight(rowMeasureHeight)
        setStatus(MEASURE_STATUS.MEASURE_WALKING)
      }
    }
  }, [status])

  // Walking measure
  React.useLayoutEffect(() => {
    if (status === MEASURE_STATUS.MEASURE_WALKING) {
      const diff = walkingIndexes[1] - walkingIndexes[0]
      const midHeight = midMeasureRef.current?.offsetHeight || 0

      if (diff > 1) {
        if (midHeight > maxHeight) {
          setWalkingIndexes([walkingIndexes[0], midIndex])
        } else {
          setWalkingIndexes([midIndex, walkingIndexes[1]])
        }
      } else {
        if (midHeight > maxHeight) {
          setWalkingIndexes([walkingIndexes[0], walkingIndexes[0]])
        } else {
          setWalkingIndexes([walkingIndexes[1], walkingIndexes[1]])
        }
        setStatus(MEASURE_STATUS.STABLE_ELLIPSIS)
      }
    }
  }, [status, walkingIndexes])

  // =========================== Render ===========================
  /** Render by cut index */
  const renderContent = (index: number) => {
    const prefixContent = contentChars.slice(0, index)
    const suffixContent = contentChars.slice(contentChars.length - index)

    return (
      <>
        {direction === 'start' && (
          <>
            {expandNode}
            {ELLIPSIS_TEXT}
          </>
        )}
        {direction !== 'start' && prefixContent.join('')}
        {direction === 'middle' && (
          <>
            {ELLIPSIS_TEXT}
            {expandNode}
            {ELLIPSIS_TEXT}
          </>
        )}
        {direction !== 'end' && suffixContent.join('')}
        {direction === 'end' && (
          <>
            {ELLIPSIS_TEXT}
            {expandNode}
          </>
        )}
      </>
    )
  }

  const finalContent = React.useMemo(() => {
    if (expanded || status === MEASURE_STATUS.STABLE_NO_ELLIPSIS) {
      return (
        <React.Fragment key='display'>
          {content}
          {status === MEASURE_STATUS.STABLE_ELLIPSIS && collapseNode}
        </React.Fragment>
      )
    }

    if (status === MEASURE_STATUS.STABLE_ELLIPSIS) {
      return renderContent(midIndex)
    }

    return null
  }, [expanded, status, content, collapseNode, midIndex])

  const allNodes = (
    <>
      {/******************* Measure Prepare *******************/}
      {/* Origin full content */}
      {status === MEASURE_STATUS.PREPARE && (
        <div key='full' aria-hidden ref={fullMeasureRef} style={measureStyle}>
          {content}
          {expandNode}
        </div>
      )}

      {/* Row measure */}
      {status === MEASURE_STATUS.PREPARE && (
        <div
          key='stable'
          aria-hidden
          ref={singleRowMeasureRef}
          style={measureStyle}
        >
          {'\\u00A0'}
        </div>
      )}

      {/******************* Measure Walking *******************/}
      {status === MEASURE_STATUS.MEASURE_WALKING && (
        <div
          key='walking-mid'
          aria-hidden
          ref={midMeasureRef}
          style={measureStyle}
        >
          {renderContent(midIndex)}
        </div>
      )}

      {/*********************** Display ***********************/}
      {/* Display content */}
      {finalContent}
    </>
  )

  return [allNodes, startMeasure] as const
}
`},69820:function(t,e){e.Z=`import './style.scss';
import * as React from 'react';
import classNames from 'classnames';

import DefaultEmptyImg from './empty';


const defaultEmptyImg = <DefaultEmptyImg />;

export interface EmptyProps {
  style?: React.CSSProperties;
  imageStyle?: React.CSSProperties;
  image?: React.ReactNode;
  description?: React.ReactNode;
  children?: React.ReactNode;
}

type CompoundedComponent = React.FC<EmptyProps>;

const Empty: CompoundedComponent = ({
  image = defaultEmptyImg,
  description,
  children,
  imageStyle,
  style,
  ...restProps
}) => {

  const prefixCls = 'triones-antm-empty'
  const des = typeof description !== 'undefined' ? description : '\u6682\u65E0\u6570\u636E';
  const alt = typeof des === 'string' ? des : 'empty';

  let imageNode: React.ReactNode = null;

  if (typeof image === 'string') {
    imageNode = <img alt={alt} src={image} />;
  } else {
    imageNode = image;
  }

  return <div className={\`\${prefixCls}-wrapper\`}>
    <div
      className={prefixCls}
      style={{ ...style }}
      {...restProps}
    >
      <div className={\`\${prefixCls}-image\`} style={imageStyle}>
        {imageNode}
      </div>
      {des && <div className={\`\${prefixCls}-description\`}>{des}</div>}
      {children && <div className={\`\${prefixCls}-footer\`}>{children}</div>}
    </div>
  </div>
};

export default Empty;
`},97855:function(t,e){e.Z=`import React from 'react'
import type { FC, ReactNode } from 'react'
import classNames from 'classnames'
import { NativeProps, withNativeProps } from '../utils/native-props'
import Divider from '../Divider'
import './style.scss'
import { mergeProps } from '../utils/with-default-props'

const classPrefix = \`triones-antm-footer\`

export type LinkItem = {
  text: string
  href: string
}

export type ChipItem = {
  text: ReactNode
  type?: 'plain' | 'link'
}

export type FooterProps = {
  label?: ReactNode
  links?: LinkItem[]
  content?: ReactNode
  chips?: ChipItem[]
  onChipClick?: (item: ChipItem, index: number) => void
  onLinkClick?: (item: LinkItem, index: number) => void
} & NativeProps<'--background-color'>

const defaultProps: FooterProps = {
  label: '',
  links: [],
  content: '',
  chips: [],
}

export const Footer: FC<FooterProps> = p => {
  const props = mergeProps(defaultProps, p) as FooterProps
  const { label, links, content, chips, onChipClick, onLinkClick } = props

  const clickChipItem = (item: ChipItem, index: number) => {
    if (chips?.length && item.type === 'link') {
      onChipClick?.(item, index)
    }
  }

  const clickLinkItem = (
    item: LinkItem,
    index: number,
    e: React.MouseEvent<HTMLAnchorElement, MouseEvent>
  ) => {
    if (onLinkClick) {
      e.preventDefault()
      onLinkClick(item, index)
    }
  }
  return withNativeProps(
    props,
    <div className={classNames(classPrefix)}>
      {label && (
        <div className={\`\${classPrefix}-label\`}>
          <Divider>{label}</Divider>
        </div>
      )}
      {!!links?.length && (
        <div className={\`\${classPrefix}-links\`}>
          {links.map((link, index) => (
            <React.Fragment key={index}>
              <a
                href={link.href}
                rel='noopener noreferrer'
                onClick={event => clickLinkItem(link, index, event)}
              >
                {link.text}
              </a>
              {index !== links.length - 1 && <Divider direction='vertical' />}
            </React.Fragment>
          ))}
        </div>
      )}
      {content && <div className={\`\${classPrefix}-content\`}>{content}</div>}
      {chips && chips.length > 0 && (
        <div className={\`\${classPrefix}-chips\`}>
          {chips.map((chip, index) => (
            <div
              key={index}
              onClick={() => clickChipItem(chip, index)}
              className={classNames(\`\${classPrefix}-chip\`, {
                [\`\${classPrefix}-chip-link\`]: chip.type === 'link',
              })}
            >
              {chip.text}
            </div>
          ))}
        </div>
      )}
    </div>
  )
}

`},76183:function(t,e){e.Z=`import { Footer } from './footer'

export type { FooterProps, ChipItem, LinkItem } from './footer'

export default Footer

`},48365:function(t,e){e.Z=`import React from 'react'
import type { FC, ReactNode, CSSProperties } from 'react'
import { NativeProps, toCSSLength, withNativeProps } from '../utils/native-props'

import './style.scss'
import { mergeProps } from '../utils/with-default-props'

const classPrefix = \`triones-antm-grid\`

export type GridProps = {
  columns: number
  gap?: number | string | [number | string, number | string]
  children?: ReactNode
} & NativeProps<'--gap' | '--gap-vertical' | '--gap-horizontal'>

export const Grid: FC<GridProps> = props => {
  const style: GridProps['style'] & Record<'--columns', string> = {
    '--columns': props.columns.toString(),
  }
  const { gap } = props
  if (gap !== undefined) {
    if (Array.isArray(gap)) {
      style['--gap-horizontal'] = toCSSLength(gap[0])
      style['--gap-vertical'] = toCSSLength(gap[1])
    } else {
      style['--gap'] = toCSSLength(gap)
    }
  }

  return withNativeProps(
    props,
    <div className={classPrefix} style={style}>
      {props.children}
    </div>
  )
}

export type GridItemProps = {
  span?: number
  onClick?: (event: React.MouseEvent<HTMLDivElement, MouseEvent>) => void
  children?: ReactNode
} & NativeProps

type GridItemStyle = CSSProperties &
  Record<'--item-span', GridItemProps['span']>

export const GridItem: FC<GridItemProps> = p => {
  const props = mergeProps({ span: 1 }, p) as GridItemProps

  const itemStyle: GridItemStyle = {
    '--item-span': props.span,
  }
  return withNativeProps(
    props,
    <div
      className={\`\${classPrefix}-item\`}
      style={itemStyle}
      onClick={props.onClick}
    >
      {props.children}
    </div>
  )
}
`},29231:function(t,e){e.Z=`import {Grid as InternalGrid, GridItem} from './grid'

export type {GridProps, GridItemProps} from './grid'

type CompoundedComponent = typeof InternalGrid & {
  Item: typeof GridItem;
};

const Grid = InternalGrid as CompoundedComponent;
Grid.Item = GridItem;

export default Grid
`},87122:function(t,e){e.Z=`import React, { memo } from 'react';
import { NativeProps, withNativeProps } from '../utils/native-props';

export const CheckIcon = memo<NativeProps>((props) => {
  return withNativeProps(
    props,
    <svg viewBox="0 0 40 40">
      <path
        d="M31.5541766,15 L28.0892433,15 L28.0892434,15 C27.971052,15 27.8576674,15.044522 27.7740471,15.1239792 L18.2724722,24.1625319 L13.2248725,19.3630279 L13.2248725,19.3630279 C13.1417074,19.2834412 13.0287551,19.2384807 12.9107898,19.2380079 L9.44474455,19.2380079 L9.44474454,19.2380079 C9.19869815,19.2384085 8.99957935,19.4284738 9,19.66253 C9,19.7747587 9.04719253,19.8823283 9.13066188,19.9616418 L17.0907466,27.5338228 C17.4170809,27.8442545 17.8447695,28 18.2713393,28 L18.2980697,28 C18.7168464,27.993643 19.133396,27.8378975 19.4530492,27.5338228 L31.8693384,15.7236361 L31.8693384,15.7236361 C32.0434167,15.5582251 32.0435739,15.2898919 31.8696892,15.1242941 C31.7860402,15.0446329 31.6725052,15 31.5541421,15 L31.5541766,15 Z"
        fill="currentColor"
      />
    </svg>,
  );
});
`},83581:function(t,e){e.Z=`import React from 'react';

export const BrokenImageIcon = () => (
  <svg viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
    <path
      d="M19.233 6.233 17.42 9.08l-10.817.001a.665.665 0 0 0-.647.562l-.007.096V34.9l5.989-8.707a2.373 2.373 0 0 1 1.801-1.005 2.415 2.415 0 0 1 1.807.625l.126.127 4.182 4.525 2.267-3.292 5.461 7.841-4.065 7.375H6.604c-1.86 0-3.382-1.47-3.482-3.317l-.005-.192V9.744c0-1.872 1.461-3.405 3.296-3.505l.19-.005h12.63Zm22.163 0c1.86 0 3.382 1.472 3.482 3.314l.005.192v29.14a3.507 3.507 0 0 1-3.3 3.505l-.191.006H27.789l3.63-6.587.06-.119a1.87 1.87 0 0 0-.163-1.853l-6.928-9.949 3.047-4.422a2.374 2.374 0 0 1 1.96-1.01 2.4 2.4 0 0 1 1.86.87l.106.14L42.05 34.89V9.74a.664.664 0 0 0-.654-.658H21.855l1.812-2.848h17.73Zm-28.305 5.611c.794 0 1.52.298 2.07.788l-.843 1.325-.067.114a1.87 1.87 0 0 0 .11 1.959l.848 1.217c-.556.515-1.3.83-2.118.83a3.122 3.122 0 0 1-3.117-3.116 3.119 3.119 0 0 1 3.117-3.117Z"
      fill="#DBDBDB"
      fillRule="nonzero"
    />
  </svg>
);
`},42080:function(t,e){e.Z=`import React from 'react';

export const ImageIcon = () => (
  <svg viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
    <path
      d="M41.396 6.234c1.923 0 3.487 1.574 3.487 3.505v29.14c0 1.937-1.568 3.51-3.491 3.51H6.604c-1.923 0-3.487-1.573-3.487-3.51V9.745c0-1.936 1.564-3.51 3.487-3.51Zm0 2.847H6.604c-.355 0-.654.3-.654.658V34.9l5.989-8.707a2.373 2.373 0 0 1 1.801-1.005 2.405 2.405 0 0 1 1.933.752l4.182 4.525 7.58-11.005a2.374 2.374 0 0 1 1.96-1.01c.79 0 1.532.38 1.966 1.01L42.05 34.89V9.74a.664.664 0 0 0-.654-.658Zm-28.305 2.763a3.119 3.119 0 0 1 3.117 3.117 3.119 3.119 0 0 1-3.117 3.117 3.122 3.122 0 0 1-3.117-3.117 3.119 3.119 0 0 1 3.117-3.117Z"
      fill="#DBDBDB"
      fillRule="nonzero"
    />
  </svg>
);
`},76890:function(t,e){e.Z=`import type { ReactNode } from 'react';
import React, { useEffect, useRef, useState } from 'react';
import { staged } from 'staged-components';
import { NativeProps, withNativeProps } from '../utils/native-props';
import { toCSSLength } from '../utils/to-css-length';
import { useIsomorphicUpdateLayoutEffect } from '../utils/use-isomorphic-update-layout-effect';
import { mergeProps } from '../utils/with-default-props';
import { BrokenImageIcon } from './broken-image-icon';
import { ImageIcon } from './image-icon';
import { LazyDetector } from './lazy-detector';

const classPrefix = \`triones-antm-image\`;

export type ImageProps = {
  src?: string;
  alt?: string;
  width?: number | string;
  height?: number | string;
  fit?: 'contain' | 'cover' | 'fill' | 'none' | 'scale-down';
  placeholder?: ReactNode;
  fallback?: ReactNode;
  lazy?: boolean;
  draggable?: boolean;
  onClick?: (event: React.MouseEvent<HTMLImageElement, Event>) => void;
  onError?: (event: React.SyntheticEvent<HTMLImageElement, Event>) => void;
  onLoad?: (event: React.SyntheticEvent<HTMLImageElement, Event>) => void;
  onContainerClick?: (event: React.MouseEvent<HTMLDivElement, Event>) => void;
} & NativeProps<'--width' | '--height'> &
  Pick<
    React.ImgHTMLAttributes<HTMLImageElement>,
    | 'crossOrigin'
    | 'decoding'
    | 'loading'
    | 'referrerPolicy'
    | 'sizes'
    | 'srcSet'
    | 'useMap'
    | 'id'
  >;

const defaultProps = {
  fit: 'fill',
  placeholder: (
    <div className={\`\${classPrefix}-tip\`}>
      <ImageIcon />
    </div>
  ),
  fallback: (
    <div className={\`\${classPrefix}-tip\`}>
      <BrokenImageIcon />
    </div>
  ),
  lazy: false,
  draggable: false,
};

export const Image = staged<ImageProps>((p) => {
  const props = mergeProps(defaultProps, p);

  const [loaded, setLoaded] = useState(false);
  const [failed, setFailed] = useState(false);

  const ref = useRef<HTMLDivElement>(null);
  const imgRef = useRef<HTMLImageElement>(null);

  let src: string | undefined = props.src;
  let srcSet: string | undefined = props.srcSet;

  const [initialized, setInitialized] = useState(!props.lazy);

  src = initialized ? props.src : undefined;
  srcSet = initialized ? props.srcSet : undefined;

  useIsomorphicUpdateLayoutEffect(() => {
    setLoaded(false);
    setFailed(false);
  }, [src]);

  useEffect(() => {
    // for nextjs ssr
    if (imgRef.current?.complete) {
      setLoaded(true);
    }
  }, []);

  function renderInner() {
    if (failed) {
      return <>{props.fallback}</>;
    }
    const img = (
      <img
        ref={imgRef}
        id={props.id}
        className={\`\${classPrefix}-img\`}
        src={src}
        alt={props.alt}
        onClick={props.onClick}
        onLoad={(e) => {
          setLoaded(true);
          props.onLoad?.(e);
        }}
        onError={(e) => {
          setFailed(true);
          props.onError?.(e);
        }}
        style={{
          objectFit: props.fit,
          display: loaded ? 'block' : 'none',
        }}
        crossOrigin={props.crossOrigin}
        decoding={props.decoding}
        loading={props.loading}
        referrerPolicy={props.referrerPolicy}
        sizes={props.sizes}
        srcSet={srcSet}
        useMap={props.useMap}
        draggable={props.draggable}
      />
    );
    return (
      <>
        {!loaded && props.placeholder}
        {img}
      </>
    );
  }

  const style: ImageProps['style'] = {};
  if (props.width) {
    style['--width'] = toCSSLength(props.width);
    style['width'] = toCSSLength(props.width);
  }
  if (props.height) {
    style['--height'] = toCSSLength(props.height);
    style['height'] = toCSSLength(props.height);
  }
  return withNativeProps(
    props,
    <div
      ref={ref}
      className={classPrefix}
      style={style}
      onClick={props.onContainerClick}
    >
      {props.lazy && !initialized && (
        <LazyDetector
          onActive={() => {
            setInitialized(true);
          }}
        />
      )}
      {renderInner()}
    </div>,
  );
});
`},64528:function(t,e){e.Z=`import { Image } from './image';
import './style.scss';

export type { ImageProps } from './image';

export default Image;
`},76172:function(t,e){e.Z=`import { useInViewport } from 'ahooks';
import type { FC } from 'react';
import React, { useEffect, useRef } from 'react';

type Props = {
  onActive: () => void;
};

export const LazyDetector: FC<Props> = (props) => {
  const ref = useRef<HTMLDivElement>(null);
  const [inViewport] = useInViewport(ref);

  useEffect(() => {
    if (inViewport) {
      props.onActive();
    }
  }, [inViewport]);

  return <div ref={ref} />;
};
`},69751:function(t,e){e.Z=`import { useMemoizedFn } from 'ahooks';
import type { FC } from 'react';
import React, { useEffect, useRef } from 'react';

interface Props {
  type: 'checkbox' | 'radio';
  checked: boolean;
  onChange: (checked: boolean) => void;
  disabled?: boolean;
  id?: string;
}

export const NativeInput: FC<Props> = (props) => {
  const inputRef = useRef<HTMLInputElement>(null);
  const handleClick = useMemoizedFn((e: MouseEvent) => {
    e.stopPropagation();
    e.stopImmediatePropagation();
    const latestChecked = (e.target as HTMLInputElement).checked;
    if (latestChecked === props.checked) return;
    props.onChange(latestChecked);
  });
  useEffect(() => {
    if (props.disabled) return;
    if (!inputRef.current) return;
    const input = inputRef.current;
    input.addEventListener('click', handleClick);
    return () => {
      input.removeEventListener('click', handleClick);
    };
  }, [props.disabled, props.onChange]);

  return (
    <input
      ref={inputRef}
      type={props.type}
      checked={props.checked}
      onChange={() => {}}
      disabled={props.disabled}
      id={props.id}
    />
  );
};
`},59914:function(t,e){e.Z=`import { NavBar } from './nav-bar'

export type { NavBarProps } from './nav-bar'

export default NavBar
`},80415:function(t,e){e.Z=`import classNames from 'classnames'
import type { FC, ReactNode } from 'react'
import React from 'react'
import { NativeProps, withNativeProps } from '../utils/native-props'
import { useConfig } from '../ConfigProvider'
import { mergeProp, mergeProps } from '../utils/with-default-props'
import { CloseOutline } from '@trionesdev/antd-taro-icons-react'

import './style.scss'

const classPrefix = \`triones-antm-nav-bar\`

export type NavBarProps = {
  back?: ReactNode
  backIcon?: boolean | ReactNode
  /**
   * @deprecated use \`backIcon\` instead
   */
  backArrow?: boolean | ReactNode
  left?: ReactNode
  right?: ReactNode
  onBack?: () => void
  children?: ReactNode
} & NativeProps<'--height' | '--border-bottom'>

const defaultBackIcon = <CloseOutline />

export const NavBar: FC<NavBarProps> = props => {
  const { navBar: componentConfig = {} } = useConfig()
  const mergedProps = mergeProps(componentConfig, props)
  const { back, backIcon, backArrow } = mergedProps

  const mergedDefaultBackIcon = componentConfig.backIcon || defaultBackIcon

  const mergedBackIcon = mergeProp<ReactNode>(
    defaultBackIcon,
    componentConfig.backIcon,
    backArrow === true ? mergedDefaultBackIcon : backArrow,
    backIcon === true ? mergedDefaultBackIcon : backIcon
  )

  return withNativeProps(
    mergedProps,
    <div className={classNames(classPrefix)}>
      <div className={\`\${classPrefix}-left\`} role='button'>
        {back !== null && (
          <div className={\`\${classPrefix}-back\`} onClick={mergedProps.onBack}>
            {mergedBackIcon && (
              <span className={\`\${classPrefix}-back-arrow\`}>
                {mergedBackIcon}
              </span>
            )}
            <span aria-hidden='true'>{back}</span>
          </div>
        )}
        {mergedProps.left}
      </div>
      <div className={\`\${classPrefix}-title\`}>{mergedProps.children}</div>
      <div className={\`\${classPrefix}-right\`}>{mergedProps.right}</div>
    </div>
  )
}
`},62146:function(t,e){e.Z=`
import { NoticeBar } from './notice-bar'

export type { NoticeBarProps } from './notice-bar'

export default NoticeBar
`},66130:function(t,e){e.Z=`import { useTimeout } from 'ahooks'
import classNames from 'classnames'
import type { ReactNode } from 'react'
import React, { memo, useRef, useState } from 'react'
import { NativeProps, withNativeProps } from '../utils/native-props'
import { useConfig } from '../ConfigProvider'
import { mergeProp, mergeProps } from '../utils/with-default-props'
import { useResizeEffect } from '../utils/use-resize-effect'
import { useMutationEffect } from '../utils/use-mutation-effect'
import { CloseCircleOutline, SetOutline } from '@trionesdev/antd-taro-icons-react'

import './style.scss'

const classPrefix = \`triones-antm-notice-bar\`

export type NoticeBarProps = {
  /** The type of the NoticeBar */
  color?: 'default' | 'alert' | 'error' | 'success' | 'info'
  /** TDelay to start scrolling, unit ms */
  delay?: number
  /** Scroll speed, unit px/s */
  speed?: number
  /** The content of the NoticeBar */
  content: ReactNode
  /** Whether it can be closed */
  closeable?: boolean
  /** Custom close icon */
  closeIcon?: ReactNode
  /** Callback when closed */
  onClose?: () => void
  /** Event when click */
  onClick?: () => void
  /** Additional operating area, displayed to the left of the close button */
  extra?: ReactNode
  /** Radio icon on the left */
  icon?: ReactNode
  /** Whether to display multiple lines */
  wrap?: boolean
  /** Block shape */
  shape?: 'rectangular' | 'neutral' | 'rounded'
  /** Border visibility */
  bordered?: 'block' | boolean
} & NativeProps<
  | '--background-color'
  | '--border-color'
  | '--text-color'
  | '--font-size'
  | '--icon-font-size'
  | '--height'
>

const defaultProps = {
  color: 'default',
  delay: 2000,
  speed: 50,
  icon: <SetOutline />,
  wrap: false,
  shape: 'rectangular',
  bordered: 'block' as 'block' | boolean,
}

export const NoticeBar = memo<NoticeBarProps>(props => {
  const { noticeBar: componentConfig = {} } = useConfig()
  const mergedProps = mergeProps(defaultProps, componentConfig, props)
  const closeIcon = mergeProp(
    <CloseCircleOutline className={\`\${classPrefix}-close-icon\`} />,
    componentConfig.closeIcon,
    props.closeIcon
  )

  const containerRef = useRef<HTMLSpanElement>(null)
  const textRef = useRef<HTMLSpanElement>(null)

  const [visible, setVisible] = useState(true)

  const speed = mergedProps.speed

  const delayLockRef = useRef(true)
  const animatingRef = useRef(false)

  function start() {
    if (delayLockRef.current || mergedProps.wrap) return

    const container = containerRef.current
    const text = textRef.current
    if (!container || !text) return

    if (container.offsetWidth >= text.offsetWidth) {
      animatingRef.current = false
      text.style.removeProperty('transition-duration')
      text.style.removeProperty('transform')
      return
    }

    if (animatingRef.current) return

    const initial = !text.style.transform
    text.style.transitionDuration = '0s'
    if (initial) {
      text.style.transform = 'translateX(0)'
    } else {
      text.style.transform = \`translateX(\${container.offsetWidth}px)\`
    }
    const distance = initial
      ? text.offsetWidth
      : container.offsetWidth + text.offsetWidth
    animatingRef.current = true
    text.style.transitionDuration = \`\${Math.round(distance / speed)}s\`
    text.style.transform = \`translateX(-\${text.offsetWidth}px)\`
  }

  useTimeout(() => {
    delayLockRef.current = false
    start()
  }, mergedProps.delay)

  useResizeEffect(() => {
    start()
  }, containerRef)

  useMutationEffect(
    () => {
      start()
    },
    textRef,
    {
      subtree: true,
      childList: true,
      characterData: true,
    }
  )

  if (!visible) return null

  return withNativeProps(
    mergedProps,
    <div
      className={classNames(
        classPrefix,
        \`\${classPrefix}-\${mergedProps.color}\`,
        \`\${classPrefix}-\${mergedProps.shape}\`,
        {
          [\`\${classPrefix}-wrap\`]: mergedProps.wrap,
          [\`\${classPrefix}-bordered\`]: mergedProps.bordered === true,
          [\`\${classPrefix}-without-border\`]: mergedProps.bordered === false,
        }
      )}
      onClick={mergedProps.onClick}
    >
      {mergedProps.icon && (
        <span className={\`\${classPrefix}-left\`}>{mergedProps.icon}</span>
      )}
      <span ref={containerRef} className={\`\${classPrefix}-content\`}>
        <span
          onTransitionEnd={() => {
            animatingRef.current = false
            start()
          }}
          ref={textRef}
          className={\`\${classPrefix}-content-inner\`}
        >
          {mergedProps.content}
        </span>
      </span>
      {(mergedProps.closeable || mergedProps.extra) && (
        <span className={\`\${classPrefix}-right\`}>
          {mergedProps.extra}
          {mergedProps.closeable && (
            <div
              className={\`\${classPrefix}-close\`}
              onClick={() => {
                setVisible(false)
                mergedProps.onClose?.()
              }}
            >
              {closeIcon}
            </div>
          )}
        </span>
      )}
    </div>
  )
})
`},87223:function(t,e){e.Z=`$class-prefix-notice-bar: 'triones-antm-notice-bar';

.#{$class-prefix-notice-bar} {
  --background-color: var(--triones-antm-color-weak);
  --border-color: var(--triones-antm-color-weak);
  --text-color: var(--triones-antm-color-text-light-solid);
  --font-size: var(--triones-antm-font-size-7);
  --icon-font-size: var(--triones-antm-font-size-10);
  --height: 40px;
  --triones-antm-notice-bar-border-radius: 4px;
  --triones-antm-notice-bar-border-width: 1px;

  height: var(--height);
  box-sizing: border-box;
  font-size: var(--font-size);
  padding: 0 12px;
  display: flex;
  align-items: center;
  justify-content: space-between;

  border: solid var(--triones-antm-notice-bar-border-width) var(--border-color);
  border-left-width: 0;
  border-right-width: 0;
  background-color: var(--background-color);
  color: var(--text-color);
  & > span[role='img'] {
    color: var(--text-color);
  }

  &.#{$class-prefix-notice-bar}-alert {
    --background-color: #fff9ed;
    --border-color: #fff3e9;
    --text-color: var(--triones-antm-color-orange);
  }
  &.#{$class-prefix-notice-bar}-error {
    --background-color: var(--triones-antm-color-danger);
    --border-color: #d9281e;
    --text-color: #fff;
  }
  &.#{$class-prefix-notice-bar}-info {
    --background-color: #d0e4ff;
    --border-color: #bcd8ff;
    --text-color: var(--triones-antm-color-primary);
  }
  &.#{$class-prefix-notice-bar}-success {
    --background-color: #d1fff0;
    --border-color: #a8f0d8;
    --text-color: var(--triones-antm-color-success);
  }

  & .#{$class-prefix-notice-bar}-left {
    flex-shrink: 0;
    margin-right: 8px;
    font-size: var(--icon-font-size);
  }
  & .#{$class-prefix-notice-bar}-content {
    flex: 1;
    overflow: hidden;
    position: relative;
    height: 100%;
    display: flex;
    align-items: center;

    & .#{$class-prefix-notice-bar}-content-inner {
      width: auto;
      transition-timing-function: linear;
      white-space: nowrap;
    }
  }

  & .#{$class-prefix-notice-bar}-right {
    display: flex;
    align-items: center;
    flex-shrink: 0;
    margin-left: 12px;
  }

  &-close {
    width: 24px;
    height: 24px;
    margin-right: -3px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--triones-antm-font-size-10);
  }

  &-wrap {
    height: auto;
    align-items: flex-start;
    padding-top: 8px;
    padding-bottom: 8px;
    line-height: 22px;
  }

  &-neutral {
    border-radius: var(--triones-antm-notice-bar-border-radius);
    -webkit-border-radius: var(--triones-antm-notice-bar-border-radius);
    -moz-border-radius: var(--triones-antm-notice-bar-border-radius);
    -ms-border-radius: var(--triones-antm-notice-bar-border-radius);
    -o-border-radius: var(--triones-antm-notice-bar-border-radius);
  }

  &-rounded {
    border-radius: 1000px;
  }

  &-bordered {
    border-left-width: var(--triones-antm-notice-bar-border-width);
    border-right-width: var(--triones-antm-notice-bar-border-width);
  }

  &-without-border {
    border-top-width: 0;
    border-bottom-width: 0;
  }
}

.#{$class-prefix-notice-bar}.#{$class-prefix-notice-bar}-wrap {
  .#{$class-prefix-notice-bar}-content {
    .#{$class-prefix-notice-bar}-content-inner {
      white-space: normal;
    }
  }
}
`},45326:function(t,e){e.Z=`import { PageIndicator } from './page-indicator'

export type { PageIndicatorProps } from './page-indicator'

export default PageIndicator
`},98188:function(t,e){e.Z=`import React, { memo } from 'react'
import type { ReactElement } from 'react'
import classNames from 'classnames'
import { NativeProps, withNativeProps } from '../utils/native-props'
import { mergeProps } from '../utils/with-default-props'
import './style.scss'

const classPrefix = \`triones-antm-page-indicator\`

export type PageIndicatorProps = {
  total: number
  current: number
  direction?: 'horizontal' | 'vertical'
  color?: 'primary' | 'white'
} & NativeProps<
  | '--dot-color'
  | '--active-dot-color'
  | '--dot-size'
  | '--active-dot-size'
  | '--dot-border-radius'
  | '--active-dot-border-radius'
  | '--dot-spacing'
>

const defaultProps = {
  color: 'primary',
  direction: 'horizontal',
}

export const PageIndicator = memo<PageIndicatorProps>(p => {
  const props = mergeProps(defaultProps, p)

  const dots: ReactElement[] = []
  for (let i = 0; i < props.total; i++) {
    dots.push(
      <div
        key={i}
        className={classNames(\`\${classPrefix}-dot\`, {
          [\`\${classPrefix}-dot-active\`]: props.current === i,
        })}
      />
    )
  }

  return withNativeProps(
    props,
    <div
      className={classNames(
        classPrefix,
        \`\${classPrefix}-\${props.direction}\`,
        \`\${classPrefix}-color-\${props.color}\`
      )}
    >
      {dots}
    </div>
  )
})
`},70431:function(t,e){e.Z=`import {Picker} from "./picker"

export default Picker
`},23115:function(t,e){e.Z=`import React, {FC, useEffect, useState} from "react"
import Popup from "../Popup";
import classNames from "classnames";
import "./style.scss"
import _ from "lodash";
import PickerView, {PickerColumnOption} from "../PickerView";

const pickerCls = "triones-antm-picker";

export type PickerProps = {
  open?: boolean
  afterOpenChange?: (open: boolean) => void;
  title?: React.ReactNode
  columns?: PickerColumnOption[][]
  /**
   * @description \u662F\u5426\u628A\u6BCF\u4E2A\u9009\u9879\u7684 label \u5305\u88C5\u5230 value \u4E2D\uFF0C\u4F1A\u628A Select \u7684 value \u7C7B\u578B\u4ECE string \u53D8\u4E3A { value: string, label: ReactNode } \u7684\u683C\u5F0F
   * @default false
   */
  labelInValue?: boolean
  value?: any[],
  onOk: (value: any[]) => void,
  onCancel?: () => void,
  onClose?: () => void,
}

export const Picker: FC<PickerProps> = React.memo(({
                                                     open,
                                                     afterOpenChange,
                                                     title,
                                                     columns,
                                                     labelInValue = false,
                                                     value, onOk,
                                                     onCancel,
                                                     onClose
                                                   }) => {
  const [internalValue, setInternalValue] = useState(value || Array.from({length: _.size(columns)}).map(() => null))
  const [innerOpen, setInnerOpen] = React.useState(open || false);
  const handleClose = () => {
    setInnerOpen(false)
    onClose?.()
  }
  const handleOk = () => {
    onOk?.(internalValue)
    handleClose()
  }

  const handelCancel = () => {
    onCancel?.()
    handleClose()
  }

  useEffect(() => {
    afterOpenChange?.(innerOpen)
  }, [innerOpen]);

  useEffect(() => {
    if (open == undefined) {
      return;
    }
    if (open == innerOpen) {
      return;
    }
    setInnerOpen(open)
  }, [open]);

  return <Popup open={innerOpen} afterOpenChange={(o)=>{setInnerOpen(o)}} styles={{
    body: {
      borderTopLeftRadius: 6,
      borderTopRightRadius: 6,
    }
  }}>
    <div className={classNames(pickerCls)}>
      <div className={classNames(\`\${pickerCls}-header\`)}>
        <a className={classNames(\`\${pickerCls}-header-button\`)} onClick={handelCancel}>\u53D6\u6D88</a>
        {title && <div className={classNames(\`\${pickerCls}-header-title\`)}>{title}</div>}
        <a className={classNames(\`\${pickerCls}-header-button\`)} onClick={handleOk}>\u786E\u5B9A</a>
      </div>
      <div className={classNames(\`\${pickerCls}-body\`)}>
        <PickerView columns={columns}
                    labelInValue={labelInValue}
                    value={internalValue}
                    onChange={(v) => {
                      setInternalValue(v)
                    }}/>
      </div>
    </div>
  </Popup>
})
`},93452:function(t,e){e.Z=`import {PickerView} from "./picker-view"
import {PickerColumnOption} from "./types"
export type {
  PickerColumnOption
}
export default PickerView
`},59105:function(t,e){e.Z=`import classNames from "classnames";
import React, {FC} from "react";
import { PickerColumnOption} from "./types";

const pickerViewCls = "triones-antm-picker-view";

type PickerViewColumnItemProps = {
  option: PickerColumnOption
}
export const PickerViewColumnItem: FC<PickerViewColumnItemProps> = ({

                                                                      option,
                                                                    }) => {
  return <div className={classNames(\`\${pickerViewCls}-column-item\`)}>
    <div className={\`\${pickerViewCls}-column-item-label\`}>
      {option.label}
    </div>
  </div>
}
`},26585:function(t,e){e.Z=`import React, {FC, useEffect, useState} from "react"
import classNames from "classnames";
import {PickerViewColumnItem} from "./picker-view-column-item";
import _ from "lodash";
import {PickerColumnOption} from "./types";

const pickerViewCls = "triones-antm-picker-view";

type PickerViewColumnProps = {
  options?: PickerColumnOption[]
  labelInValue?: boolean
  value?: any
  onChange?: (value: any, index?: number) => void
}

export const PickerViewColumn: FC<PickerViewColumnProps> = React.memo(
  ({
     options,
     labelInValue,
     value,
     onChange,
   }) => {
    const [internalValue, setInternalValue] = useState<any>(value || null);
    const itemHeight = 34;
    const [touchPoint, setTouchPoint] = React.useState<any>();
    const [translateY, setTranslateY] = useState(0)

    const handleSelectOption = (selectValue: any) => {
      const valueIndex = _.findIndex(options, (option) => {
        if (labelInValue) {
          return option.value === selectValue.value;
        }
        return option.value === selectValue
      })
      setTranslateY(0 - (valueIndex * itemHeight));
    }

    useEffect(() => {
      if (value == undefined) {
        return;
      }
      if (value == internalValue) {
        return;
      }
      setInternalValue(value)
      handleSelectOption(value)
    }, [value]);

    useEffect(() => {
      if (!value) {
        const selectValue = labelInValue ? options?.[0] : options?.[0].value
        setInternalValue(selectValue || null);
        onChange?.(selectValue || null);
        handleSelectOption(selectValue);
      } else {
        handleSelectOption(value);
      }
    }, [options]);

    return <div className={classNames(\`\${pickerViewCls}-column\`)}>
      <div className={classNames(\`\${pickerViewCls}-column-wheel\`)}
           style={{transform: \`translate3d(0, \${translateY}px, 0)\`}}
           onTouchStart={(event) => {
             const startPoint = {clientX: event.touches[0].clientX, clientY: event.touches[0].clientY}
             setTouchPoint(startPoint);
           }}
           onTouchMove={(event) => {
             const movePoint = {clientX: event.touches[0].clientX, clientY: event.touches[0].clientY}
             setTranslateY(translateY + (movePoint.clientY - touchPoint.clientY));
             setTouchPoint(movePoint);
           }}
           onTouchEnd={(event) => {
             let selectIndex = 0;
             if (translateY > 0) {
               setTranslateY(0);
               selectIndex = 0;
             } else if (translateY < (0 - itemHeight * (_.size(options) - 1))) {
               setTranslateY(0 - itemHeight * (_.size(options) - 1));
               selectIndex = _.size(options) - 1;
             } else {
               const num = Math.abs(Math.round(translateY / itemHeight));
               setTranslateY(0 - num * itemHeight);
               selectIndex = num;
             }
             console.log("selectIndex", selectIndex)
             const selectValue = labelInValue ? options?.[selectIndex] : options?.[selectIndex].value
             setInternalValue(selectValue);
             onChange?.(selectValue);
           }}
      >
        {!_.isEmpty(options) && options?.map((option: any, index: number) => {
          return <PickerViewColumnItem option={option} key={\`view-column-\${index}\`}/>
        })}
      </div>
    </div>
  }
)
`},40871:function(t,e){e.Z=`import classNames from "classnames";
import _ from "lodash";
import {PickerViewColumn} from "./picker-view-column";
import React, {FC, useEffect, useState} from "react";
import { PickerColumnOption} from "./types";
import "./style.scss"

const pickerViewCls = "triones-antm-picker-view";
type PickerViewProps = {
  columns?: PickerColumnOption[][]
  labelInValue?: boolean
  value?: any[]
  onChange?: (value: any[]) => void
}
export const PickerView: FC<PickerViewProps> = React.memo(({columns, labelInValue, value, onChange}) => {
  const [internalValue, setInternalValue] = useState(value || Array.from({length: _.size(columns)}).map(() => null))
  useEffect(() => {
    if (value == undefined) {
      return;
    }
    if (value == internalValue) {
      return;
    }
    setInternalValue(value);
  }, [value]);
  return <div className={classNames(\`\${pickerViewCls}\`)}>
    {!_.isEmpty(columns) && columns?.map((column: any, index: number) => {
      return <PickerViewColumn key={\`column-\${index}\`} labelInValue={labelInValue}
                               options={column}
                               value={value?.[index]}
                               onChange={(v) => {
                                 internalValue[index] = v;
                                 onChange?.(internalValue)
                               }}
      />
    })}
    <div className={classNames(\`\${pickerViewCls}-mask\`)}>
      <div className={classNames(\`\${pickerViewCls}-mask-top\`)}/>
      <div className={classNames(\`\${pickerViewCls}-mask-middle\`)}/>
      <div className={classNames(\`\${pickerViewCls}-mask-bottom\`)}/>
    </div>
  </div>
})
`},31223:function(t,e){e.Z=`import {Popover} from "./popover"
export default Popover;
`},93703:function(t,e){e.Z=`import React from "react";
import {FC} from "react";

type PopoverProps = {}
export const Popover: FC<PopoverProps> = () => {
    return <></>
}
`},81891:function(t,e){e.Z=`import {Popup,PopupProps} from "./popup";

export type {PopupProps}
export default Popup;
`},67353:function(t,e){e.Z=`import React, {CSSProperties, FC, PropsWithChildren, useEffect} from "react";
import classNames from "classnames";
import "./style.scss"
import {CloseOutline} from "@trionesdev/antd-taro-icons-react/src";

const popupCls = "triones-antm-popup";

export type Position = "top" | "bottom" | "left" | "right";


export type PopupModalProps = {
  /**
   * @description \u6837\u5F0F
   * @default horizontal
   */
  styles?: {
    body?: CSSProperties;
  }
  /**
   * @description Modal \u5B8C\u5168\u5173\u95ED\u540E\u7684\u56DE\u8C03
   * @default
   */
  afterClose?: () => void;
  /**
   * @description \u662F\u5426\u6253\u5F00
   * @default false
   */
  open?: boolean,
  /**
   * @description \u6253\u5F00\u4F4D\u7F6E
   * @default bottom
   */
  position?: Position
  /**
   * @description \u662F\u5426\u53EF\u4EE5\u5173\u95ED(\u663E\u793A\u5173\u95ED\u6309\u94AE)
   * @default false
   */
  closable?: boolean
  /**
   * @description \u662F\u5426\u8499\u5C42\u5173\u95ED
   * @default true
   */
  maskClosable?: boolean
  /**
   * @description \u5173\u95ED\u65F6\u9500\u6BC1 Modal \u91CC\u7684\u5B50\u5143\u7D20
   * @default false
   */
  destroyOnClose?: boolean
  /**
   * @description \u8BBE\u7F6E Modal \u7684 z-index
   * @default 1000
   */
  zIndex?: number;
  /**
   * @description \u6253\u5F00\u548C\u5173\u95ED Modal \u65F6\u52A8\u753B\u7ED3\u675F\u540E\u7684\u56DE\u8C03
   * @default
   */
  afterOpenChange?: (open: boolean) => void;
  onDestroy?: () => void;

}

export const PopupModal: FC<PropsWithChildren<PopupModalProps>> = ({
                                                                     children,
                                                                     styles,
                                                                     afterClose,
                                                                     open,
                                                                     position = 'bottom',
                                                                     closable = false,
                                                                     maskClosable = true,
                                                                     destroyOnClose = false,
                                                                     zIndex = 1000,
                                                                     afterOpenChange,
                                                                     onDestroy
                                                                   }) => {
  const [internalOpen, setInternalOpen] = React.useState<boolean>(open || false);
  const handleClose = () => {
    setInternalOpen(false);
    afterClose?.()
  }

  useEffect(() => {
    afterOpenChange?.(internalOpen!);
    if (!internalOpen) {
      if (destroyOnClose) {
        onDestroy?.()
      }
    }
  }, [internalOpen]);

  useEffect(() => {
    if (open == undefined) {
      return
    }
    if (open == internalOpen) {
      return;
    }
    setInternalOpen(open);
  }, [open]);

  return <div className={classNames(popupCls)} style={{zIndex, display: internalOpen ? 'block' : 'none'}}>
    <div className={classNames(\`\${popupCls}-mask\`)} onClick={() => {
      if (maskClosable) {
        handleClose();
      }
    }}></div>
    <div className={classNames(\`\${popupCls}-body\`, \`\${popupCls}-\${position}\`)} style={styles?.body}>
      {closable && <>
        <CloseOutline className={classNames(\`\${popupCls}-close\`)} onClick={() => {
          handleClose()
        }}/>
      </>}
      {children}
    </div>
  </div>
}
`},30226:function(t,e){e.Z=`import React, {FC, PropsWithChildren, useEffect} from "react";
import {createPortal} from "react-dom";
import {PopupModal, PopupModalProps} from "./popup-modal";


export type PopupProps = Omit<PopupModalProps, 'onDestroy'> & {
  /**
   * @description \u83B7\u53D6\u5BB9\u5668
   * @default null
   */
  getContainer?: () => HTMLElement,
}
export const Popup: FC<PropsWithChildren<PopupProps>> = ({
                                                           children,
                                                           getContainer,
                                                           open = false,
                                                           ...rest
                                                         }) => {
  const [renderEnable, setRenderEnable] = React.useState(false);
  const containerRef = React.useRef<HTMLDivElement>(null);
  const mountRef = React.useRef<HTMLDivElement | null>(null);
  const rootContainer = getContainer?.() || containerRef.current!;

  const Portal = () => {
    if (!mountRef.current) {
      mountRef.current = document.createElement("div");
      rootContainer.appendChild(mountRef.current);
    }

    const destroy = () => {
      if (rootContainer && mountRef.current) {
        rootContainer.removeChild(mountRef.current);
      }
      mountRef.current = null;
      setRenderEnable(false);
    }

    return <>{createPortal(<PopupModal {...rest} onDestroy={destroy}
                                       open={open}>{children}</PopupModal>, mountRef.current!)}</>

  }

  useEffect(() => {
    if (open && !renderEnable) {
      setRenderEnable(true);
    }
  }, [open]);


  return <>
    {!getContainer?.() && <div ref={containerRef}></div>}
    {renderEnable && <Portal/>}</>;
}
`},8576:function(t,e){e.Z=`import { ProgressBar } from './progress-bar'; 
export default ProgressBar;`},46793:function(t,e){e.Z=`import classNames from 'classnames';
import React, { FC } from 'react';
import { NativeProps, withNativeProps } from '../utils/native-props';
import { mergeProps } from '../utils/with-default-props';
import './style.scss';

type ProgressBarProps = {
  /**
   * \u8FDB\u5EA6\u767E\u5206\u6BD4
   */
  percent: number;
  /**
   * \u662F\u5426\u4E3A\u5706\u89D2
   */
  rounded?: boolean;
  /**
   * \u8FDB\u5EA6\u6587\u672C
   */
  text?: boolean | React.ReactNode | ((percent: number) => React.ReactNode);
  /**
   * \u81EA\u5B9A\u4E49\u7C7B\u540D
   */
  className?: string;
  /**
   * \u81EA\u5B9A\u4E49\u6837\u5F0F
   */
  style?: React.CSSProperties;
} & NativeProps<
  '--fill-color' | '--track-color' | '--track-width' | '--text-width'
>;

const defaultProps = {
  rounded: true,
};

export const ProgressBar: FC<ProgressBarProps> = (p) => {
  const props = mergeProps(defaultProps, p) as ProgressBarProps;
  const { percent, rounded, className, style, text } = props;

  return withNativeProps(
    props,
    <div className={classNames('triones-antm-progress-bar', className)}>
      <div
        className="triones-antm-progress-bar-track"
        style={{
          ...style,
          borderRadius: rounded ? \`var(--track-width)\` : '0',
        }}
      >
        <div
          className="triones-antm-progress-bar-fill"
          style={{
            width: \`\${percent}%\`,
            borderRadius: rounded ? \`var(--track-width)\` : '0',
          }}
        />
      </div>
      {text && (
        <div className="triones-antm-progress-bar-text">
          {typeof text === 'function'
            ? text(percent)
            : text === true
              ? \`\${percent}%\`
              : text}
        </div>
      )}
    </div>,
  );
};
`},15311:function(t,e){e.Z=`import { createContext } from 'react';
import { RadioValue } from './radio';

export const RadioGroupContext = createContext<{
  value: RadioValue[];
  disabled: boolean;
  check: (val: RadioValue) => void;
  uncheck: (val: RadioValue) => void;
} | null>(null);
`},8286:function(t,e){e.Z=`import type { FC, ReactNode } from 'react';
import React from 'react';
import { usePropsValue } from '../utils/use-props-value';
import { mergeProps } from '../utils/with-default-props';
import { RadioGroupContext } from './group-context';
import { RadioValue } from './radio';

export interface RadioGroupProps {
  value?: RadioValue | null;
  onChange?: (val: RadioValue) => void;
  defaultValue?: RadioValue | null;
  disabled?: boolean;
  children?: ReactNode;
}

const defaultProps = {
  disabled: false,
  defaultValue: null,
};

export const Group: FC<RadioGroupProps> = (p) => {
  const props = mergeProps(defaultProps, p);
  const [value, setValue] = usePropsValue({
    value: props.value,
    defaultValue: props.defaultValue,
    onChange: (v) => {
      if (v === null) return;
      props.onChange?.(v);
    },
  });
  return (
    <RadioGroupContext.Provider
      // TODO: \u6027\u80FD\u4F18\u5316
      value={{
        value: value === null ? [] : [value],
        check: (v) => {
          setValue(v);
        },
        uncheck: () => {},
        disabled: props.disabled,
      }}
    >
      {props.children}
    </RadioGroupContext.Provider>
  );
};
`},54007:function(t,e){e.Z=`import { attachPropertiesToComponent } from '../utils/attach-properties-to-component';
import { Group } from './group';
import { Radio } from './radio';
import './style.scss';

export type { RadioGroupProps } from './group';
export type { RadioProps, RadioValue } from './radio';

export default attachPropertiesToComponent(Radio, {
  Group,
});
`},33591:function(t,e){e.Z=`import classNames from 'classnames';
import type { FC, ReactNode } from 'react';
import React, { useContext } from 'react';
import { CheckIcon } from '../Icon/check-icon';
import { NativeInput } from '../Input/native-input';
import { devWarning } from '../utils/dev-log';
import { isDev } from '../utils/is-dev';
import { NativeProps, withNativeProps } from '../utils/native-props';
import { usePropsValue } from '../utils/use-props-value';
import { mergeProps } from '../utils/with-default-props';
import { RadioGroupContext } from './group-context';

const classPrefix = \`triones-antm-radio\`;

export type RadioValue = string | number;

export type RadioProps = {
  checked?: boolean;
  defaultChecked?: boolean;
  disabled?: boolean;
  onChange?: (checked: boolean) => void;
  value?: RadioValue;
  block?: boolean;
  id?: string;
  icon?: (checked: boolean) => ReactNode;
  children?: ReactNode;
  onClick?: (event: React.MouseEvent<HTMLLabelElement, MouseEvent>) => void;
} & NativeProps<'--icon-size' | '--font-size' | '--gap'>;

const defaultProps = {
  defaultChecked: false,
};

export const Radio: FC<RadioProps> = (p) => {
  const props = mergeProps(defaultProps, p);
  const groupContext = useContext(RadioGroupContext);

  let [checked, setChecked] = usePropsValue<boolean>({
    value: props.checked,
    defaultValue: props.defaultChecked,
    onChange: props.onChange,
  }) as [boolean, (v: boolean) => void];
  let disabled = props.disabled;

  const { value } = props;
  if (groupContext && value !== undefined) {
    if (isDev) {
      if (p.checked !== undefined) {
        devWarning(
          'Radio',
          'When used within \`Radio.Group\`, the \`checked\` prop of \`Radio\` will not work.',
        );
      }
      if (p.defaultChecked !== undefined) {
        devWarning(
          'Radio',
          'When used within \`Radio.Group\`, the \`defaultChecked\` prop of \`Radio\` will not work.',
        );
      }
    }

    checked = groupContext.value.includes(value);
    setChecked = (innerChecked: boolean) => {
      if (innerChecked) {
        groupContext.check(value);
      } else {
        groupContext.uncheck(value);
      }
      props.onChange?.(innerChecked);
    };
    disabled = disabled || groupContext.disabled;
  }

  const renderIcon = () => {
    if (props.icon) {
      return (
        <div className={\`\${classPrefix}-custom-icon\`}>
          {props.icon(checked)}
        </div>
      );
    }

    return (
      <div className={\`\${classPrefix}-icon\`}>{checked && <CheckIcon />}</div>
    );
  };

  return withNativeProps(
    props,
    <label
      onClick={props.onClick}
      className={classNames(classPrefix, {
        [\`\${classPrefix}-checked\`]: checked,
        [\`\${classPrefix}-disabled\`]: disabled,
        [\`\${classPrefix}-block\`]: props.block,
      })}
    >
      <NativeInput
        type="radio"
        checked={checked}
        onChange={setChecked}
        disabled={disabled}
        id={props.id}
      />
      {renderIcon()}
      {props.children && (
        <div className={\`\${classPrefix}-content\`}>{props.children}</div>
      )}
    </label>,
  );
};
`},77635:function(t,e){e.Z=`import { Rate } from "./rate"

export default Rate;`},54898:function(t,e){e.Z=`import { StarFill } from '@trionesdev/antd-taro-icons-react';
import classNames from 'classnames';
import React, { FC, useState } from 'react';
import { NativeProps, withNativeProps } from '../utils/native-props';
import { mergeProps } from '../utils/with-default-props';
import './style.scss';

type RateProps = {
  /**
   * \u8BC4\u5206\u503C
   */
  value?: number;
  /**
   * \u9ED8\u8BA4\u8BC4\u5206\u503C
   */
  defaultValue?: number;
  /**
   * \u8BC4\u5206\u603B\u6570
   */
  count?: number;
  /**
   * \u662F\u5426\u5141\u8BB8\u534A\u661F
   */
  allowHalf?: boolean;
  /**
   * \u662F\u5426\u5141\u8BB8\u6E05\u9664
   */
  allowClear?: boolean;
  /**
   * \u662F\u5426\u53EA\u8BFB
   */
  readonly?: boolean;
  /**
   * \u8BC4\u5206\u6539\u53D8\u65F6\u7684\u56DE\u8C03
   */
  onChange?: (value: number) => void;
  /**
   * \u81EA\u5B9A\u4E49\u7C7B\u540D
   */
  className?: string;
  /**
   * \u661F\u661F\u5927\u5C0F
   */
  size?: number;
  /**
   * \u81EA\u5B9A\u4E49\u5B57\u7B26
   */
  character?: React.ReactNode;
  /**
   * \u81EA\u5B9A\u4E49\u6837\u5F0F
   */
  style?: React.CSSProperties;
} & NativeProps<'--active-color' | '--inactive-color'>;

const defaultProps = {
  count: 5,
  defaultValue: 0,
  allowHalf: false,
  readonly: false,
  allowClear: true,
  size: 20,
};

export const Rate: FC<RateProps> = (p) => {
  const props = mergeProps(defaultProps, p);
  const {
    value,
    defaultValue,
    count,
    allowHalf,
    readonly,
    allowClear,
    size,
    character,
    onChange,
  } = props;
  const [internalValue, setInternalValue] = useState(defaultValue);
  const clsPrefix = 'triones-antm-rate';

  const currentValue = value ?? internalValue;

  const handleClick = (index: number, isHalf: boolean) => {
    if (readonly) return;
    const newValue = isHalf ? index + 0.5 : index + 1;

    if (allowClear) {
      if (
        (!allowHalf && currentValue === newValue) ||
        (allowHalf &&
          currentValue === newValue &&
          currentValue === index + (isHalf ? 0.5 : 1))
      ) {
        if (!value) {
          setInternalValue(0);
        }
        onChange?.(0);
        return;
      }
    }

    if (!value) {
      setInternalValue(newValue);
    }
    onChange?.(newValue);
  };

  return withNativeProps(
    props,
    <div
      className={classNames(clsPrefix, {
        [\`\${clsPrefix}-readonly\`]: readonly,
      })}
    >
      {[...Array(count)].map((_, index) => (
        <div
          key={index}
          className={\`\${clsPrefix}-star-wrapper\`}
          onClick={(e) => {
            if (!allowHalf) {
              handleClick(index, false);
              return;
            }

            const rect = e.currentTarget.getBoundingClientRect();
            const isHalf = e.clientX - rect.left < rect.width / 2;
            handleClick(index, isHalf);
          }}
        >
          {character ? (
            <>
              <div
                className={classNames(\`\${clsPrefix}-star\`, {
                  [\`\${clsPrefix}-star-active\`]: index + 1 <= currentValue,
                })}
                style={{
                  fontSize: size,
                }}
              >
                {character}
              </div>
              {allowHalf && (
                <div
                  className={classNames(
                    \`\${clsPrefix}-star\`,
                    \`\${clsPrefix}-star-half-item\`,
                    {
                      [\`\${clsPrefix}-star-active\`]: index + 0.5 <= currentValue,
                    },
                  )}
                  style={{
                    fontSize: size,
                  }}
                >
                  {character}
                </div>
              )}
            </>
          ) : (
            <>
              <StarFill
                className={classNames(\`\${clsPrefix}-star\`, {
                  [\`\${clsPrefix}-star-active\`]: index + 1 <= currentValue,
                })}
                style={{
                  fontSize: size,
                }}
              />
              {allowHalf && (
                <StarFill
                  className={classNames(
                    \`\${clsPrefix}-star\`,
                    \`\${clsPrefix}-star-half-item\`,
                    {
                      [\`\${clsPrefix}-star-active\`]: index + 0.5 <= currentValue,
                    },
                  )}
                  style={{
                    fontSize: size,
                  }}
                />
              )}
            </>
          )}
        </div>
      ))}
    </div>
  );
};
`},83671:function(t,e){e.Z=`
import { Result } from './result'

export type { ResultProps } from './result'

export default Result
`},44096:function(t,e){e.Z=`import React from 'react'
import type { FC, ReactNode } from 'react'
import classNames from 'classnames'
import { useResultIcon } from './use-result-icon'
import { NativeProps, withNativeProps } from '../utils/native-props'
import { mergeProps } from '../utils/with-default-props'
import './style.scss'

const classPrefix = \`triones-antm-result\`

const defaultProps = {
  status: 'info',
}

export type ResultProps = {
  status?: 'success' | 'error' | 'info' | 'waiting' | 'warning'
  title: ReactNode
  description?: ReactNode
  icon?: ReactNode
} & NativeProps

export const Result: FC<ResultProps> = p => {
  const props = mergeProps(defaultProps, p)
  const { status, title, description, icon } = props
  const fallbackIcon = useResultIcon(status)
  if (!status) return null

  return withNativeProps(
    props,
    <div className={classNames(classPrefix, \`\${classPrefix}-\${status}\`)}>
      <div className={\`\${classPrefix}-icon\`}>{icon || fallbackIcon}</div>
      <div className={\`\${classPrefix}-title\`}>{title}</div>
      {!!description && (
        <div className={\`\${classPrefix}-description\`}>{description}</div>
      )}
    </div>
  )
}
`},87621:function(t,e){e.Z=`import React from 'react'
import { useConfig } from '../ConfigProvider'
import { CheckCircleOutline, CloseCircleOutline, InformationCircleOutline } from '@trionesdev/antd-taro-icons-react'


export const useResultIcon = (
  status?: 'success' | 'error' | 'info' | 'waiting' | 'warning'
) => {
  const { result: componentConfig = {} } = useConfig()
  const {
    successIcon = <CheckCircleOutline />,
    errorIcon = <CloseCircleOutline />,
    infoIcon = <InformationCircleOutline />,
    waitingIcon = <InformationCircleOutline />,
    warningIcon = <InformationCircleOutline />,
  } = componentConfig || {}
  switch (status) {
    case 'success':
      return successIcon
    case 'error':
      return errorIcon
    case 'info':
      return infoIcon
    case 'waiting':
      return waitingIcon
    case 'warning':
      return warningIcon
    default:
      return null
  }
}
`},57151:function(t,e){e.Z=`import { Switch } from './switch'

export type { SwitchProps } from './switch'

export default Switch
`},14452:function(t,e){e.Z=`import React, { memo } from 'react'
import { NativeProps, withNativeProps } from '../utils/native-props'

export const SpinIcon = memo<NativeProps>(props => {
  return withNativeProps(
    props,
    <svg width='28px' height='28px' viewBox='0 0 28 28'>
      <g stroke='none' strokeWidth='1' fill='none' fillRule='evenodd'>
        <g transform='translate(-137.000000, -840.000000)' fill='#1576FE'>
          <g transform='translate(80.000000, 823.000000)'>
            <g transform='translate(53.000000, 13.000000)'>
              <path d='M17.9996753,31.5 C10.5556724,31.5 4.5,25.4443275 4.5,18.0003247 C4.5,10.5563219 10.5556724,4.5 17.9996753,4.5 C18.5355492,4.5 18.9702974,4.93474816 18.9702974,5.47062208 C18.9702974,6.006496 18.5355492,6.44124416 17.9996753,6.44124416 C11.6261524,6.44124416 6.44124416,11.6267709 6.44124416,18.0002938 C6.44124416,24.3738167 11.6261524,29.5587249 17.9996753,29.5587249 C24.3731982,29.5587249 29.5587249,24.3738167 29.5587249,18.0002938 C29.5587249,14.7964616 28.2778291,11.8169616 25.9523687,9.61220279 C25.5637302,9.24317094 25.5473089,8.62893223 25.9157222,8.23967523 C26.2841356,7.84976878 26.8989928,7.83461537 27.2882498,8.20302872 C30.0042351,10.7787368 31.5,14.2580826 31.5,18.0002938 C31.5,25.4443275 25.4436781,31.5 17.9996753,31.5 Z' />
            </g>
          </g>
        </g>
      </g>
    </svg>
  )
})
`},19635:function(t,e){e.Z=`import classNames from 'classnames'
import React, { useState } from 'react'
import type { FC, ReactNode } from 'react'
import { SpinIcon } from './spin-icon'
import { NativeProps, withNativeProps } from '../utils/native-props'
import { mergeProps } from '../utils/with-default-props'
import { useConfig } from '../ConfigProvider'
import { usePropsValue } from '../utils/use-props-value'
import { isPromise } from '../utils/validate'

import './style.scss'

const classPrefix = \`triones-antm-switch\`

export type SwitchProps = {
  loading?: boolean
  disabled?: boolean
  checked?: boolean
  defaultChecked?: boolean
  /** @deprecated use \`onChange\` instead */
  beforeChange?: (val: boolean) => Promise<void>
  onChange?: (checked: boolean) => void | Promise<void>
  checkedText?: ReactNode
  uncheckedText?: ReactNode
} & NativeProps<'--checked-color' | '--width' | '--height' | '--border-width'>

const defaultProps = {
  defaultChecked: false,
}

export const Switch: FC<SwitchProps> = p => {
  const props = mergeProps(defaultProps, p)
  const disabled = props.disabled || props.loading || false
  const [changing, setChanging] = useState(false)
  const { locale } = useConfig()

  const [checked, setChecked] = usePropsValue({
    value: props.checked,
    defaultValue: props.defaultChecked,
    onChange: props.onChange,
  })

  async function onClick() {
    if (disabled || props.loading || changing) {
      return
    }
    const nextChecked = !checked
    if (props.beforeChange) {
      setChanging(true)
      try {
        await props.beforeChange(nextChecked)
        setChanging(false)
      } catch (e) {
        setChanging(false)
        throw e
      }
    }
    const result = setChecked(nextChecked)
    if (isPromise(result)) {
      setChanging(true)
      try {
        await result
        setChanging(false)
      } catch (e) {
        setChanging(false)
        throw e
      }
    }
  }

  return withNativeProps(
    props,
    <div
      onClick={onClick}
      className={classNames(classPrefix, {
        [\`\${classPrefix}-checked\`]: checked,
        [\`\${classPrefix}-disabled\`]: disabled || changing,
      })}
      role='switch'
      aria-label={locale.Switch.name}
      aria-checked={checked}
      aria-disabled={disabled}
    >
      <div className={\`\${classPrefix}-checkbox\`}>
        <div className={\`\${classPrefix}-handle\`}>
          {(props.loading || changing) && (
            <SpinIcon className={\`\${classPrefix}-spin-icon\`} />
          )}
        </div>
        <div className={\`\${classPrefix}-inner\`}>
          {checked ? props.checkedText : props.uncheckedText}
        </div>
      </div>
    </div>
  )
}
`},31347:function(t,e){e.Z=`import { TabBar as InternalTabBar, TabBarItem } from "./tab-bar"

type CompoundedComponent = typeof InternalTabBar & {
  Item: typeof TabBarItem
}

const TabBar = InternalTabBar as CompoundedComponent

export default TabBar
`},53336:function(t,e){e.Z=`import "./index.scss"
import React, {FC} from "react";
import classNames from "classnames";

type TabBarItemType = {
  key: string
  label?: React.ReactNode
  icon?: React.ReactNode
  activeIcon?: React.ReactNode
  disabled?: boolean
  active?: boolean
  activeKey?: string
  onClick?: () => void
}

type TabBarItemProps = Omit<TabBarItemType, 'key'> & {}

export type TabBarProps = {
  items: TabBarItemType[]
  onClick?: (e: { item?: TabBarItemType }) => void
}
const tabBarCls = 'triones-tab-bar'
export const TabBarItem: FC<TabBarItemProps> = ({
                                                  label,
                                                  icon,
                                                  activeIcon,
                                                  activeKey,
                                                  active,
                                                  onClick,
                                                  ...props
                                                }) => {
  console.log(props)

  const displayIcon = active ? (activeIcon ?? icon) : icon
  return <div className={classNames(\`\${tabBarCls}-item\`, {
    [\`\${tabBarCls}-item-active\`]: active
  })} onClick={() => onClick?.()}>
    <div>{displayIcon}</div>
    <div className={classNames(\`\${tabBarCls}-item-label\`)}>{label}</div>
  </div>
}


export const TabBar: FC<TabBarProps> = ({items, onClick}) => {
  const [innerActiveKey, setInnerActiveKey] = React.useState<string | undefined>()


  return <div className={classNames(tabBarCls)}>
    <div className={classNames(\`\${tabBarCls}-wrap\`)}>
      {items.map(item => <TabBarItem {...item} activeKey={innerActiveKey} active={item.key === innerActiveKey}
                                     onClick={() => {
                                       onClick?.({item})
                                       setInnerActiveKey(item.key)
                                       console.log()
                                       console.log(item)
                                     }}/>)}
    </div>
  </div>
}
`},87990:function(t,e){e.Z=`import { Tag } from "./tag"

export default Tag `},31257:function(t,e){e.Z=`import classNames from 'classnames';
import React, { FC } from 'react';
import './style.scss';

type TagProps = {
  children?: React.ReactNode;
  /**
   * \u6807\u7B7E\u8272
   */
  color?: 'default' | 'primary' | 'success' | 'warning' | 'danger' | string;
  /**
   * \u586B\u5145\u6A21\u5F0F
   */
  fill?: 'solid' | 'outline';
  /**
   * \u70B9\u51FB\u65F6\u7684\u56DE\u8C03
   */
  onClick?: (e: React.MouseEvent<HTMLElement>) => void;
  /**
   * \u662F\u5426\u5706\u89D2
   */
  round?: boolean;
  /**
   * \u81EA\u5B9A\u4E49\u7C7B\u540D
   */
  className?: string;
  /**
   * \u81EA\u5B9A\u4E49\u6837\u5F0F
   */
  style?: React.CSSProperties;
};

export const Tag: FC<TagProps> = ({
  children,
  color = 'default',
  fill = 'solid',
  onClick,
  className,
  style,
  round = false,
  ...props
}) => {
  const clsPrefix = 'triones-antm-tag';

  // \u662F\u5426\u662F\u9884\u8BBE\u989C\u8272
  const isPresetColor = [
    'default',
    'primary',
    'success',
    'warning',
    'danger',
  ].includes(color);

  const customStyle = !isPresetColor
    ? {
        ...(fill === 'solid'
          ? {
              backgroundColor: color,
              color: '#fff',
            }
          : {
              color: color,
              borderColor: color,
              backgroundColor: 'transparent',
            }),
        ...style,
      }
    : style;

  return (
    <div
      className={classNames(
        clsPrefix,
        {
          [\`\${clsPrefix}-\${color}\`]: isPresetColor,
          [\`\${clsPrefix}-\${fill}\`]: fill,
          [\`\${clsPrefix}-\${round ? 'round' : 'square'}\`]: round,
        },
        className,
      )}
      style={customStyle}
      {...props}
      onClick={onClick}
    >
      {children}
    </div>
  );
};
`},46099:function(t,e){e.Z=`import { WaterMark } from './water-mark'

export type { WaterMarkProps } from './water-mark'

export default WaterMark
`},60053:function(t,e){e.Z=`import classNames from 'classnames'
import React, { useEffect, useState } from 'react'
import type { FC } from 'react'
import { NativeProps, withNativeProps } from '../utils/native-props'
import { mergeProps } from '../utils/with-default-props'

import './style.scss'

const classPrefix = \`triones-antm-water-mark\`

export type WaterMarkProps = {
  gapX?: number
  gapY?: number
  zIndex?: number
  width?: number
  height?: number
  rotate?: number
  image?: string
  imageWidth?: number
  imageHeight?: number
  content?: string | string[]
  fontColor?: string
  fontStyle?: 'none' | 'normal' | 'italic' | 'oblique'
  fontFamily?: string
  fontWeight?: 'normal' | 'light' | 'weight' | number
  fontSize?: number | string
  fullPage?: boolean
} & NativeProps<'--z-index'>

const defaultProps = {
  fullPage: true,
}

export const WaterMark: FC<WaterMarkProps> = p => {
  const props = mergeProps(defaultProps, p)
  const {
    zIndex,
    gapX = 24,
    gapY = 48,
    width = 120,
    height = 64,
    rotate = -22,
    image,
    imageWidth = 120,
    imageHeight = 64,
    content,
    fontStyle = 'normal',
    fontWeight = 'normal',
    fontColor = 'rgba(0,0,0,.15)',
    fontSize = 14,
    fontFamily = 'sans-serif',
  } = props

  const [base64Url, setBase64Url] = useState('')

  useEffect(() => {
    const canvas = document.createElement('canvas')
    const ratio = window.devicePixelRatio
    const ctx = canvas.getContext('2d')

    const canvasWidth = \`\${(gapX + width) * ratio}px\`
    const canvasHeight = \`\${(gapY + height) * ratio}px\`

    const markWidth = width * ratio
    const markHeight = height * ratio

    canvas.setAttribute('width', canvasWidth)
    canvas.setAttribute('height', canvasHeight)

    if (ctx) {
      if (image) {
        ctx.translate(markWidth / 2, markHeight / 2)
        ctx.rotate((Math.PI / 180) * Number(rotate))

        const img = new Image()
        img.crossOrigin = 'anonymous'
        img.referrerPolicy = 'no-referrer'
        img.onload = () => {
          ctx.drawImage(
            img,
            (-imageWidth * ratio) / 2,
            (-imageHeight * ratio) / 2,
            imageWidth * ratio,
            imageHeight * ratio
          )
          ctx.restore()
          setBase64Url(canvas.toDataURL())
        }
        img.src = image
      } else if (content) {
        ctx.textBaseline = 'middle'
        ctx.textAlign = 'center'
        // \u6587\u5B57\u7ED5\u4E2D\u95F4\u65CB\u8F6C
        ctx.translate(markWidth / 2, markHeight / 2)
        ctx.rotate((Math.PI / 180) * Number(rotate))

        const markSize = Number(fontSize) * ratio
        ctx.font = \`\${fontStyle} normal \${fontWeight} \${markSize}px/\${markHeight}px \${fontFamily}\`
        ctx.fillStyle = fontColor
        if (Array.isArray(content)) {
          content.forEach((item: string, index: number) =>
            ctx.fillText(item, 0, index * markSize)
          )
        } else {
          ctx.fillText(content, 0, 0)
        }
        ctx.restore()
        setBase64Url(canvas.toDataURL())
      }
    } else {
      throw new Error('Canvas is not supported in the current environment')
    }
  }, [
    gapX,
    gapY,
    rotate,
    fontStyle,
    fontWeight,
    width,
    height,
    fontFamily,
    fontColor,
    image,
    content,
    fontSize,
  ])

  return withNativeProps(
    props,
    <div
      className={classNames(classPrefix, {
        [\`\${classPrefix}-full-page\`]: props.fullPage,
      })}
      style={{
        zIndex,
        backgroundSize: \`\${gapX + width}px\`,

        // Not give \`url\` if its empty. Which will cause 404 error.
        backgroundImage: base64Url === '' ? undefined : \`url('\${base64Url}')\`,
      }}
    />
  )
}
`},37687:function(t,e){e.Z=`import './style';

export {default as Avatar} from './Avatar';
export {default as Badge} from './Badge';
export {default as Button} from './Button';
export type {ButtonProps} from './Button';
export {default as Card} from './Card';
export {default as Checkbox} from './Checkbox';
export {default as ConfigProvider} from './ConfigProvider';
export {default as Divider} from './Divider';
export {default as Ellipsis} from './Ellipsis';
export {default as Empty} from './Empty';
export {default as ErrorBlock} from './ErrorBlock';
export {default as Footer} from './Footer';
export {default as Grid} from './Grid';
export {default as Image} from './Images';
export {default as Input} from './Input';
export type {InputProps, InputPasswordProps, InputOPTProps} from './Input';
export {default as NavBar} from './NavBar';
export {default as NoticeBar} from './NoticeBar';
export {default as PageIndicator} from './PageIndicator';
export {default as Popover} from './Popover';
export {default as ProgressBar} from './ProgressBar';
export {default as Radio} from './Radio';
export {default as Rate} from './Rate';
export {default as Result} from './Result';
export {default as Space} from './Space';
export {default as Switch} from './Switch';
export {default as TabBar} from './TabBar';
export {default as Tag} from './Tag';
export {default as WaterMark} from './WaterMark';

export {default as Form} from './Form';
export type {FormProps, FormItemProps} from "./Form"
export {default as Popup} from './Popup'
export type {PopupProps} from './Popup'
export {default as Picker} from './Picker'
export {default as PickerView} from './PickerView'

`},57982:function(t,e){e.Z=`const typeTemplate = '\${label} is not a valid \${type}'

export const base = {
  locale: 'en',
  common: {
    confirm: 'Confirm',
    cancel: 'Cancel',
    loading: 'Loading',
    close: 'Close',
  },
  Calendar: {
    title: 'Date selection',
    confirm: 'Confirm',
    start: 'start',
    end: 'end',
    today: 'today',
    markItems: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
    yearAndMonth: '\${year}/\${month}',
  },
  Cascader: {
    placeholder: 'Selecting',
  },
  Dialog: {
    ok: 'OK',
  },
  DatePicker: {
    tillNow: 'Till Now',
  },
  ErrorBlock: {
    default: {
      title: 'Oops, something went wrong',
      description: 'Please wait a minute and try again',
    },
    busy: {
      title: 'Oops, not loading',
      description: 'Try to refresh the page',
    },
    disconnected: {
      title: 'Network is busy',
      description: 'Try to refresh the page',
    },
    empty: {
      title: "Hmm, couldn't find that...",
      description: 'Want to try a new search?',
    },
  },
  Form: {
    required: 'Required',
    optional: 'Optional',
    defaultValidateMessages: {
      default: 'Field validation error for \${label}',
      required: 'Please enter \${label}',
      enum: '\${label} must be one of [\${enum}]',
      whitespace: '\${label} cannot be a blank character',
      date: {
        format: '\${label} date format is invalid',
        parse: '\${label} cannot be converted to a date',
        invalid: '\${label} is an invalid date',
      },
      types: {
        string: typeTemplate,
        method: typeTemplate,
        array: typeTemplate,
        object: typeTemplate,
        number: typeTemplate,
        date: typeTemplate,
        boolean: typeTemplate,
        integer: typeTemplate,
        float: typeTemplate,
        regexp: typeTemplate,
        email: typeTemplate,
        url: typeTemplate,
        hex: typeTemplate,
      },
      string: {
        len: '\${label} must be \${len} characters',
        min: '\${label} must be at least \${min} characters',
        max: '\${label} must be up to \${max} characters',
        range: '\${label} must be between \${min}-\${max} characters',
      },
      number: {
        len: '\${label} must be equal to \${len}',
        min: '\${label} must be minimum \${min}',
        max: '\${label} must be maximum \${max}',
        range: '\${label} must be between \${min}-\${max}',
      },
      array: {
        len: 'Must be \${len} \${label}',
        min: 'At least \${min} \${label}',
        max: 'At most \${max} \${label}',
        range: 'The amount of \${label} must be between \${min}-\${max}',
      },
      pattern: {
        mismatch: '\${label} does not match the pattern \${pattern}',
      },
    },
  },
  ImageUploader: {
    uploading: 'Uploading...',
    upload: 'Upload',
  },
  InfiniteScroll: {
    noMore: 'No more',
    failedToLoad: 'Failed to load',
    retry: 'Retry',
  },
  Input: {
    clear: 'clear',
  },
  Mask: {
    name: 'Mask',
  },
  Modal: {
    ok: 'OK',
  },
  PasscodeInput: {
    name: 'Passcode Input',
  },
  PullToRefresh: {
    pulling: 'Scroll down to refresh',
    canRelease: 'Release to refresh immediately',
    complete: 'Refresh successful',
  },
  SearchBar: {
    name: 'Search Bar',
  },
  Slider: {
    name: 'Slider',
  },
  Stepper: {
    decrease: 'decrease',
    increase: 'increase',
  },
  Switch: {
    name: 'Switch',
  },
  Selector: {
    name: 'Selector',
  },
}

export type Locale = typeof base
`},67198:function(t,e){e.Z=`import { mergeLocale } from '../utils/merge-locale'
import { base } from './base'

const enUS = mergeLocale(base, {})

export default enUS
`},96334:function(t,e){e.Z=`import './style.scss';

const canUseDom = !!(
  typeof window !== 'undefined' &&
  typeof document !== 'undefined' &&
  window.document &&
  window.document.createElement
);
if (canUseDom) {
  document.addEventListener('touchstart', () => {}, true);
}
`},28745:function(t,e){e.Z=`export function attachPropertiesToComponent<C, P extends Record<string, any>>(
  component: C,
  properties: P,
): C & P {
  const ret = component as any;
  for (const key in properties) {
    if (properties.hasOwnProperty(key)) {
      ret[key] = properties[key];
    }
  }
  return ret;
}
`},77215:function(t,e){e.Z=`import { isDev } from './is-dev';

export function devWarning(component: string, message: string): void {
  if (isDev) {
    console.warn(\`[antd-mobile: \${component}] \${message}\`);
  }
}

export function devError(component: string, message: string) {
  if (isDev) {
    console.error(\`[antd-mobile: \${component}] \${message}\`);
  }
}

let infoBox: HTMLTextAreaElement;
export function devPrint(...message: any[]) {
  if (isDev) {
    if (!infoBox) {
      infoBox = document.createElement('textarea');
      document.body.append(infoBox);
      infoBox.style.position = 'fixed';
      infoBox.style.top = '0';
      infoBox.style.left = '0';
      infoBox.style.width = '100vw';
      infoBox.style.height = '100vh';
      infoBox.style.zIndex = '999999';
      infoBox.style.fontSize = '12px';
      // infoBox.style.opacity = '0.85'
      infoBox.style.pointerEvents = 'none';
      infoBox.style.background = 'transparent';
      infoBox.style.textShadow =
        '-1px -1px 0 #FFF, -1px 1px 0 #FFF, 1px -1px 0 #FFF, 1px 1px 0 #FFF';
    }

    infoBox.value = \`\${infoBox.value}\\n\${message.join(' ')}\`.trim();
    infoBox.scrollTop = infoBox.scrollHeight;
  }
}
`},51630:function(t,e){e.Z=`export const isDev =
  process.env.NODE_ENV === 'development' || process.env.NODE_ENV === 'test';
`},18001:function(t,e){e.Z=`export function mergeLocale<T extends object, P extends object>(
  base: T,
  patch: P
): T {
  function merge(a: any, b: any): any {
    if (
      typeof a !== 'object' ||
      typeof b !== 'object' ||
      Array.isArray(a) ||
      Array.isArray(b)
    ) {
      return b !== undefined ? b : a
    }
    const result: any = {}
    for (const key in a) {
      if (a.hasOwnProperty(key)) {
        result[key] = merge(a[key], b[key])
      }
    }
    return result
  }
  return merge(base, patch)
}
`},84601:function(t,e){e.Z="export function toCSSLength(val: string | number) {\n  return typeof val === 'number' ? `${val}px` : val;\n}\n"},91857:function(t,e){e.Z=`import { createUpdateEffect, useIsomorphicLayoutEffect } from 'ahooks';

export const useIsomorphicUpdateLayoutEffect = createUpdateEffect(
  useIsomorphicLayoutEffect,
);
`},35969:function(t,e){e.Z=`import { useMemoizedFn } from 'ahooks'
import { RefObject, useEffect } from 'react'

export function observe(
  element: HTMLElement | null,
  options: MutationObserverInit,
  callback: VoidFunction
) {
  if (element && typeof MutationObserver !== 'undefined') {
    let observer: MutationObserver | null = new MutationObserver(() => {
      callback()
    })
    observer.observe(element, options)

    // Return cleanup function
    return () => {
      if (observer) {
        observer.disconnect()
        observer = null
      }
    }
  }

  return () => { }
}

export function useMutationEffect(
  effect: () => void,
  targetRef: RefObject<HTMLElement>,
  options: MutationObserverInit
) {
  const fn = useMemoizedFn(effect)
  useEffect(() => {
    const cleanup = observe(targetRef.current, options, fn)
    return cleanup
  }, [targetRef])
}
`},21629:function(t,e){e.Z=`import { useMemoizedFn, useUpdate } from 'ahooks';
import { SetStateAction, useRef } from 'react';

type Options<T> = {
  value?: T;
  defaultValue: T;
  onChange?: (v: T) => void;
};

export function usePropsValue<T>(options: Options<T>) {
  const { value, defaultValue, onChange } = options;

  const update = useUpdate();

  const stateRef = useRef<T>(value !== undefined ? value : defaultValue);
  if (value !== undefined) {
    stateRef.current = value;
  }

  const setState = useMemoizedFn(
    (v: SetStateAction<T>, forceTrigger: boolean = false) => {
      // \`forceTrigger\` means trigger \`onChange\` even if \`v\` is the same as \`stateRef.current\`
      const nextValue =
        typeof v === 'function'
          ? (v as (prevState: T) => T)(stateRef.current)
          : v;
      if (!forceTrigger && nextValue === stateRef.current) return;
      stateRef.current = nextValue;
      update();
      return onChange?.(nextValue);
    },
  );
  return [stateRef.current, setState] as const;
}
`},4684:function(t,e){e.Z=`import { RefObject } from 'react'
import { useIsomorphicLayoutEffect, useMemoizedFn } from 'ahooks'

export function useResizeEffect<T extends HTMLElement>(
  effect: (target: T) => void,
  targetRef: RefObject<T>
) {
  const fn = useMemoizedFn(effect)
  useIsomorphicLayoutEffect(() => {
    const target = targetRef.current
    if (!target) return
    if (window.ResizeObserver) {
      let animationFrame: number
      const observer = new ResizeObserver(() => {
        animationFrame = window.requestAnimationFrame(() => fn(target))
      })
      observer.observe(target)
      return () => {
        window.cancelAnimationFrame(animationFrame)
        observer.disconnect()
      }
    } else {
      fn(target)
    }
  }, [targetRef])
}
`},32792:function(t,e){e.Z=`export function isPromise(obj: unknown): obj is Promise<unknown> {
  return (
    !!obj && typeof obj === 'object' && typeof (obj as any).then === 'function'
  )
}
`},85898:function(t,e){e.Z=`import React from 'react'
import type { ReactElement } from 'react'
export type PropagationEvent = 'click' | 'touchstart'

const eventToPropRecord: Record<PropagationEvent, string> = {
  'click': 'onClick',
  'touchstart': 'onTouchStart'
}

export function withStopPropagation(
  events: PropagationEvent[],
  element: ReactElement
) {
  const props: Record<string, any> = { ...element.props }
  for (const key of events) {
    const prop = eventToPropRecord[key]
    props[prop] = function (e: Event) {
      e.stopPropagation()
      element.props[prop]?.(e)
    }
  }
  return React.cloneElement(element, props)
}
`}}]);
