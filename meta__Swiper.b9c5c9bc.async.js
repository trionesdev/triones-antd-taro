"use strict";(self.webpackChunk_trionesdev_antd_mobile_react=self.webpackChunk_trionesdev_antd_mobile_react||[]).push([[8593],{55959:function(t,e,n){var i;n.r(e),n.d(e,{demos:function(){return R}});var c=n(90819),r=n.n(c),a=n(89933),l=n.n(a),o=n(44194),d=n(17583),m=n(92876),p=n(51006),u=n(8608),_=n(3927),h=n(14887),f=n(96162),g=n(87068),v=n(51865),B=n.n(v),x=n(71348),O=n.n(x),y=n(75753),I=n(22792),E=n(52258),P=n(40336),w=n(11614),C=n(1959),b=n(61331),S=n(85590),R={"swiper-demo-base":{component:o.memo(o.lazy(function(){return n.e(1065).then(n.bind(n,5157))})),asset:{type:"BLOCK",id:"swiper-demo-base",refAtomIds:["Swiper"],dependencies:{"index.tsx":{type:"FILE",value:n(88358).Z},react:{type:"NPM",value:"18.3.1"},"../../Image.ts":{type:"FILE",value:n(50609).Z},"../../DemoBlock.tsx":{type:"FILE",value:n(3699).Z},"../index.tsx":{type:"FILE",value:n(10448).Z},"./index.scss":{type:"FILE",value:n(52964).Z},"./SwiperCore.tsx":{type:"FILE",value:n(44526).Z},"./style.scss":{type:"FILE",value:n(94126).Z},"./Swiper.tsx":{type:"FILE",value:n(6093).Z},"./image.tsx":{type:"FILE",value:n(25402).Z},classnames:{type:"NPM",value:"2.5.1"},"staged-components":{type:"NPM",value:"1.1.3"},"./lazy-detector.tsx":{type:"FILE",value:n(13779).Z},"../utils/use-isomorphic-update-layout-effect.ts":{type:"FILE",value:n(89755).Z},"../utils/with-default-props.tsx":{type:"FILE",value:n(6494).Z},"./broken-image-icon.tsx":{type:"FILE",value:n(79914).Z},"../utils/native-props.ts":{type:"FILE",value:n(51468).Z},"./image-icon.tsx":{type:"FILE",value:n(92385).Z},"../utils/to-css-length.ts":{type:"FILE",value:n(69248).Z},ahooks:{type:"NPM",value:"3.9.6"}},entry:"index.tsx"},context:{"../../Image.ts":d,"../../DemoBlock.tsx":m,"../index.tsx":p,"./index.scss":u,"./SwiperCore.tsx":_,"./style.scss":h,"./Swiper.tsx":f,"./image.tsx":g,"./lazy-detector.tsx":y,"../utils/use-isomorphic-update-layout-effect.ts":I,"../utils/with-default-props.tsx":E,"./broken-image-icon.tsx":P,"../utils/native-props.ts":w,"./image-icon.tsx":C,"../utils/to-css-length.ts":b,react:i||(i=n.t(o,2)),"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Image/index.ts":d,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/DemoBlock/index.tsx":m,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Swiper/index.tsx":p,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/DemoBlock/index.scss":u,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Swiper/SwiperCore.tsx":_,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Swiper/style.scss":h,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Swiper/Swiper.tsx":f,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Image/image.tsx":g,classnames:v,"staged-components":x,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Image/lazy-detector.tsx":y,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/utils/use-isomorphic-update-layout-effect.ts":I,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/utils/with-default-props.tsx":E,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Image/broken-image-icon.tsx":P,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/utils/native-props.ts":w,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/Image/image-icon.tsx":C,"/home/runner/work/triones-antd-taro/triones-antd-taro/packages/antd-mobile-react/src/utils/to-css-length.ts":b,ahooks:S},renderOpts:{compile:function(){var D=l()(r()().mark(function T(){var k,L=arguments;return r()().wrap(function(s){for(;;)switch(s.prev=s.next){case 0:return s.next=2,n.e(8036).then(n.bind(n,68036));case 2:return s.abrupt("return",(k=s.sent).default.apply(k,L));case 3:case"end":return s.stop()}},T)}));function M(){return D.apply(this,arguments)}return M}()}}}},92876:function(t,e,n){n.r(e),n.d(e,{DemoBlock:function(){return a}});var i=n(44194),c=n(8608),r=n(31549),a=function(o){return(0,r.jsxs)("div",{className:"demoBlock",children:[(0,r.jsx)("div",{className:"demoTitle",children:o.title}),(0,r.jsx)("div",{className:"demoMain",style:{padding:"12px 12px",background:"#ffffff"},children:o.children})]})}},8608:function(t,e,n){n.r(e)},53345:function(t,e,n){n.r(e),n.d(e,{texts:function(){return i}});const i=[]},52964:function(t,e){e.Z=`.demoBlock {
  font-size: 14px;
  margin-bottom: 12px;
  &:last-of-type {
    padding-bottom: 32px;
  }
  scrollbar-width: thin;
}

.demoTitle {
  padding: 12px 12px 8px;
  color: #697b8c;
  font-size: 14px;
  background: #fafbfc;
}

.demoMain {
  border-right: none;
  border-left: none;
}
`},3699:function(t,e){e.Z=`import React from 'react'
import type { FC, ReactNode } from 'react'
import './index.scss'

interface Props {
  title: string
  padding?: string
  background?: string
  children?: ReactNode
}

export const DemoBlock: FC<Props> = props => {
  return (
    <div className='demoBlock'>
      <div className='demoTitle'>{props.title}</div>
      <div
        className='demoMain'
        style={{
          padding: '12px 12px',
          background: '#ffffff',
        }}
      >
        {props.children}
      </div>
    </div>
  )
}
`},79914:function(t,e){e.Z=`import React from 'react';

export const BrokenImageIcon = () => (
  <svg viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
    <path
      d="M19.233 6.233 17.42 9.08l-10.817.001a.665.665 0 0 0-.647.562l-.007.096V34.9l5.989-8.707a2.373 2.373 0 0 1 1.801-1.005 2.415 2.415 0 0 1 1.807.625l.126.127 4.182 4.525 2.267-3.292 5.461 7.841-4.065 7.375H6.604c-1.86 0-3.382-1.47-3.482-3.317l-.005-.192V9.744c0-1.872 1.461-3.405 3.296-3.505l.19-.005h12.63Zm22.163 0c1.86 0 3.382 1.472 3.482 3.314l.005.192v29.14a3.507 3.507 0 0 1-3.3 3.505l-.191.006H27.789l3.63-6.587.06-.119a1.87 1.87 0 0 0-.163-1.853l-6.928-9.949 3.047-4.422a2.374 2.374 0 0 1 1.96-1.01 2.4 2.4 0 0 1 1.86.87l.106.14L42.05 34.89V9.74a.664.664 0 0 0-.654-.658H21.855l1.812-2.848h17.73Zm-28.305 5.611c.794 0 1.52.298 2.07.788l-.843 1.325-.067.114a1.87 1.87 0 0 0 .11 1.959l.848 1.217c-.556.515-1.3.83-2.118.83a3.122 3.122 0 0 1-3.117-3.116 3.119 3.119 0 0 1 3.117-3.117Z"
      fill="#DBDBDB"
      fillRule="nonzero"
    />
  </svg>
);
`},92385:function(t,e){e.Z=`import React from 'react';

export const ImageIcon = () => (
  <svg viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
    <path
      d="M41.396 6.234c1.923 0 3.487 1.574 3.487 3.505v29.14c0 1.937-1.568 3.51-3.491 3.51H6.604c-1.923 0-3.487-1.573-3.487-3.51V9.745c0-1.936 1.564-3.51 3.487-3.51Zm0 2.847H6.604c-.355 0-.654.3-.654.658V34.9l5.989-8.707a2.373 2.373 0 0 1 1.801-1.005 2.405 2.405 0 0 1 1.933.752l4.182 4.525 7.58-11.005a2.374 2.374 0 0 1 1.96-1.01c.79 0 1.532.38 1.966 1.01L42.05 34.89V9.74a.664.664 0 0 0-.654-.658Zm-28.305 2.763a3.119 3.119 0 0 1 3.117 3.117 3.119 3.119 0 0 1-3.117 3.117 3.122 3.122 0 0 1-3.117-3.117 3.119 3.119 0 0 1 3.117-3.117Z"
      fill="#DBDBDB"
      fillRule="nonzero"
    />
  </svg>
);
`},25402:function(t,e){e.Z=`import type { ReactNode } from 'react';
import React, { useEffect, useRef, useState } from 'react';
import { staged } from 'staged-components';
import { NativeProps, withNativeProps } from '../utils/native-props';
import { toCSSLength } from '../utils/to-css-length';
import { useIsomorphicUpdateLayoutEffect } from '../utils/use-isomorphic-update-layout-effect';
import { mergeProps } from '../utils/with-default-props';
import { BrokenImageIcon } from './broken-image-icon';
import { ImageIcon } from './image-icon';
import { LazyDetector } from './lazy-detector';

const classPrefix = \`triones-antm-image\`;

export type ImageProps = {
  src?: string;
  alt?: string;
  width?: number | string;
  height?: number | string;
  fit?: 'contain' | 'cover' | 'fill' | 'none' | 'scale-down';
  placeholder?: ReactNode;
  fallback?: ReactNode;
  lazy?: boolean;
  draggable?: boolean;
  onClick?: (event: React.MouseEvent<HTMLImageElement, Event>) => void;
  onError?: (event: React.SyntheticEvent<HTMLImageElement, Event>) => void;
  onLoad?: (event: React.SyntheticEvent<HTMLImageElement, Event>) => void;
  onContainerClick?: (event: React.MouseEvent<HTMLDivElement, Event>) => void;
} & NativeProps<'--width' | '--height'> &
  Pick<
    React.ImgHTMLAttributes<HTMLImageElement>,
    | 'crossOrigin'
    | 'decoding'
    | 'loading'
    | 'referrerPolicy'
    | 'sizes'
    | 'srcSet'
    | 'useMap'
    | 'id'
  >;

const defaultProps = {
  fit: 'fill',
  placeholder: (
    <div className={\`\${classPrefix}-tip\`}>
      <ImageIcon />
    </div>
  ),
  fallback: (
    <div className={\`\${classPrefix}-tip\`}>
      <BrokenImageIcon />
    </div>
  ),
  lazy: false,
  draggable: false,
};

export const Image = staged<ImageProps>((p) => {
  const props = mergeProps(defaultProps, p);

  const [loaded, setLoaded] = useState(false);
  const [failed, setFailed] = useState(false);

  const ref = useRef<HTMLDivElement>(null);
  const imgRef = useRef<HTMLImageElement>(null);

  let src: string | undefined = props.src;
  let srcSet: string | undefined = props.srcSet;

  const [initialized, setInitialized] = useState(!props.lazy);

  src = initialized ? props.src : undefined;
  srcSet = initialized ? props.srcSet : undefined;

  useIsomorphicUpdateLayoutEffect(() => {
    setLoaded(false);
    setFailed(false);
  }, [src]);

  useEffect(() => {
    // for nextjs ssr
    if (imgRef.current?.complete) {
      setLoaded(true);
    }
  }, []);

  function renderInner() {
    if (failed) {
      return <>{props.fallback}</>;
    }
    const img = (
      <img
        ref={imgRef}
        // id={props.id}
        className={\`\${classPrefix}-img\`}
        src={src}
        alt={props.alt}
        onClick={props.onClick}
        onLoad={(e) => {
          setLoaded(true);
          props.onLoad?.(e);
        }}
        onError={(e) => {
          setFailed(true);
          props.onError?.(e);
        }}
        style={{
          objectFit: props.fit,
          display: loaded ? 'block' : 'none',
        }}
        crossOrigin={props.crossOrigin}
        decoding={props.decoding}
        loading={props.loading}
        referrerPolicy={props.referrerPolicy}
        sizes={props.sizes}
        srcSet={srcSet}
        useMap={props.useMap}
        draggable={props.draggable}
      />
    );
    return (
      <>
        {!loaded && props.placeholder}
        {img}
      </>
    );
  }

  const style: ImageProps['style'] = {};
  if (props.width) {
    style['width'] = toCSSLength(props.width);
  }
  if (props.height) {
    style['height'] = toCSSLength(props.height);
  }
  return withNativeProps(
    props,
    <div
      ref={ref}
      className={classPrefix}
      style={style}
      onClick={props.onContainerClick}
    >
      {props.lazy && !initialized && (
        <LazyDetector
          onActive={() => {
            setInitialized(true);
          }}
        />
      )}
      {renderInner()}
    </div>,
  );
});
`},50609:function(t,e){e.Z=`import {Image, ImageProps } from './image';
import './style.scss';

export type { ImageProps } ;
export default Image;
`},13779:function(t,e){e.Z=`import { useInViewport } from 'ahooks';
import type { FC } from 'react';
import React, { useEffect, useRef } from 'react';

type Props = {
  onActive: () => void;
};

export const LazyDetector: FC<Props> = (props) => {
  const ref = useRef<HTMLDivElement>(null);
  const [inViewport] = useInViewport(ref);

  useEffect(() => {
    if (inViewport) {
      props.onActive();
    }
  }, [inViewport]);

  return <div ref={ref} />;
};
`},6093:function(t,e){e.Z=`import classNames from 'classnames';
import React, {FC, PropsWithChildren} from 'react';
import {SwiperCore, SwiperCoreProps} from './SwiperCore';

const cls = 'triones-antm-swiper';

export type SwiperProps = SwiperCoreProps & {
  /**
   * @description \u662F\u5426\u663E\u793A\u6307\u793A\u5668
   * @default true
   */
  indicator?: boolean;
};
export const Swiper: FC<PropsWithChildren<SwiperProps>> = ({ ...props }) => {
  const style = { ...props.style };
  if (props.height) {
    style.height = props.height;
  }

  return (
    <div className={classNames(cls, props.className)} style={style}>
      <SwiperCore {...props} />
    </div>
  );
};
`},44526:function(t,e){e.Z=`import React, {
  FC,
  PropsWithChildren,
  useEffect,
  useRef,
  useState,
} from 'react';

import './style.scss';
import classNames from 'classnames';

const cls = 'triones-antm-swiper';

export type SwiperItemType = {
  children: React.ReactNode;
};

export type SwiperCoreProps = {
  className?: string;
  style?: React.CSSProperties;
  direction?: 'horizontal' | 'vertical';
  /**
   * @description \u9AD8\u5EA6
   */
  height?: number;
  items?: SwiperItemType[];
  activeIndex?: number;
  autoplay?: boolean;
  onChange?: (value: number) => void;
};

export type SwiperCoreItemProps = {
  style?: React.CSSProperties;
};

export const SwiperCoreItem: FC<PropsWithChildren<SwiperCoreItemProps>> = ({
  children,
  style,
}) => {
  return (
    <div className={classNames(\`\${cls}-core-item\`)} style={style}>
      {children}
    </div>
  );
};

export const SwiperCore: FC<PropsWithChildren<SwiperCoreProps>> = ({
  children,
  direction = 'horizontal',
  height,
  items,
  activeIndex,
  onChange,
}) => {
  const [innerActiveIndex, setInnerActiveIndex] = useState<number>(
    activeIndex || 0,
  );
  const boxRef = React.createRef<any>();
  const boxUniqueRef = React.useRef<string>('swiper-box');
  const wrapperRef = React.createRef<HTMLDivElement>();
  const [itemWidth, setItemWidth] = useState<number>();
  const [wrapperWidth, setWrapperWidth] = useState<number>();
  const [itemHeight, setItemHeight] = useState<number | undefined>(height);
  const [wrapperHeight, setWrapperHeight] = useState<number>();
  const [count, setCount] = useState<number>(0);

  const [touching, setTouching] = useState<boolean>(false);
  const [touchPoint, setTouchPoint] = React.useState<any>();
  const [translateX, setTranslateX] = useState(0);
  const [translateY, setTranslateY] = useState(0);
  const max = 0;
  const minTranslateX = useRef<number>();
  const minTranslateY = useRef<number>();

  const computeItemWidth = async (): Promise<number | undefined> => {
    return Promise.resolve(boxRef.current?.clientWidth);
  };

  const computeItemHeight = async (): Promise<number | undefined> => {
    return Promise.resolve(boxRef.current?.clientHeight);
  };

  useEffect(() => {
    if (items?.length) {
      setCount(items.length);
    } else {
      setCount(React.Children.count(children));
    }
    if (itemWidth) {
      if (items && items?.length > 0) {
        setWrapperWidth(itemWidth * items.length);
        minTranslateX.current = -(itemWidth * (items.length - 1));
      } else {
        if (children && React.Children.count(children) > 0) {
          setWrapperWidth(itemWidth * React.Children.count(children));
          minTranslateX.current = -(
            itemWidth *
            (React.Children.count(children) - 1)
          );
        }
      }
    }
    if (itemHeight) {
      if (items && items?.length > 0) {
        setWrapperHeight(itemHeight * items.length);
        minTranslateY.current = -(itemHeight * (items.length - 1));
      } else {
        if (children && React.Children.count(children) > 0) {
          setWrapperHeight(itemHeight * React.Children.count(children));
          minTranslateY.current = -(
            itemHeight *
            (React.Children.count(children) - 1)
          );
        }
      }
    }
  }, [children, items, itemWidth, itemHeight]);

  useEffect(() => {
    if (activeIndex === undefined) {
      return;
    }
    if (activeIndex === innerActiveIndex) {
      return;
    }
    setInnerActiveIndex(activeIndex);
  }, [activeIndex]);

  useEffect(() => {
    if (direction === 'vertical') {
      if (itemHeight) {
        setTranslateY(-(innerActiveIndex * itemHeight!));
      }
    } else {
      if (itemWidth) {
        setTranslateX(-(innerActiveIndex * itemWidth!));
      }
    }
  }, [innerActiveIndex, itemWidth, itemHeight]);

  useEffect(() => {
    if (boxRef.current) {
      computeItemHeight().then((height) => {
        setItemHeight(height);
      });
      computeItemWidth().then((width) => {
        setItemWidth(width);
      });
    }
  }, []);

  const handlerRender = () => {
    if (items?.length) {
      return (
        <>
          {items.map((item, index) => {
            return (
              <SwiperCoreItem
                key={index}
                style={{ width: itemWidth, height: itemHeight }}
              >
                {item?.children}
              </SwiperCoreItem>
            );
          })}
        </>
      );
    } else {
      if (children && Array.isArray(children)) {
        return children.map((item, index) => {
          return React.cloneElement(item, {
            style: { ...item.style, width: itemWidth, height: itemHeight },
          });
        });
      }
    }
    return null;
  };

  return (
    <div className={classNames(\`\${cls}-core\`)}>
      <div
        ref={boxRef}
        id={boxRef.current?.uid}
        className={classNames(\`\${cls}-core-box\`, \`\${boxUniqueRef.current}\`)}
      >
        <div
          ref={wrapperRef}
          className={classNames(
            \`\${cls}-core-wrapper\`,
            \`\${cls}-core-wrapper-\${direction}\`,
          )}
          style={{
            width: direction === 'horizontal' ? wrapperWidth : undefined,
            height: direction === 'vertical' ? wrapperHeight : undefined,
            transform: \`translate3d(\${translateX}Px, \${translateY}Px, 0)\`,
          }}
          onTouchStart={(event) => {
            setTouching(true);
            const startPoint = {
              clientX: event.touches[0].clientX,
              clientY: event.touches[0].clientY,
            };
            setTouchPoint(startPoint);
          }}
          onTouchMove={(event) => {
            if (touching) {
              const movePoint = {
                clientX: event.touches[0].clientX,
                clientY: event.touches[0].clientY,
              };

              if (direction === 'vertical') {
                setTranslateY(
                  translateY + (movePoint.clientY - touchPoint.clientY),
                );
              } else {
                setTranslateX(
                  translateX + (movePoint.clientX - touchPoint.clientX),
                );
              }
              setTouchPoint(movePoint);
            }
          }}
          onTouchEnd={(event) => {
            setTouching(false);
            if (direction === 'vertical') {
              if (translateY > max) {
                setTranslateY(0);
                setInnerActiveIndex(0);
                onChange?.(0);
              } else if (translateY < minTranslateY.current!) {
                setTranslateY(minTranslateY.current!);
                setInnerActiveIndex(count - 1);
                onChange?.(count - 1);
              } else {
                const index = Math.round(
                  Math.abs(translateY) / Math.abs(itemHeight!),
                );
                setTranslateY(-(index * itemHeight!));
                setInnerActiveIndex(index);
                onChange?.(index);
              }
            } else {
              if (translateX > max) {
                setTranslateX(0);
                setInnerActiveIndex(0);
                onChange?.(0);
              } else if (translateX < minTranslateX.current!) {
                setTranslateX(minTranslateX.current!);
                setInnerActiveIndex(count - 1);
                onChange?.(count - 1);
              } else {
                const index = Math.round(
                  Math.abs(translateX) / Math.abs(itemWidth!),
                );
                setTranslateX(-(index * itemWidth!));
                setInnerActiveIndex(index);
                onChange?.(index);
              }
            }
          }}
        >
          {handlerRender()}
        </div>
      </div>
    </div>
  );
};
`},88358:function(t,e){e.Z=`import React from "react"
import {DemoBlock} from "../../DemoBlock";
import Swiper from "../index";
import Image from "../../Image";

export default ()=>{
  return <>
    <DemoBlock title={\`\u57FA\u672C\u4F7F\u7528\`}>
      <Swiper height={150} items={[
        {children: <div style={{height:'100%',width:'100%',background:'red',display:'flex',justifyContent:'center',alignItems:'center'}}>1</div>},
        {children: <div style={{height:'100%',width:'100%',background:'green',display:'flex',justifyContent:'center',alignItems:'center'}}>2</div>},
        {children: <div style={{height:'100%',width:'100%',background:'yellow',display:'flex',justifyContent:'center',alignItems:'center'}}>3</div>},
        {children: <div style={{height:'100%',width:'100%',background:'blue',display:'flex',justifyContent:'center',alignItems:'center'}}>4</div>},
      ]}/>
    </DemoBlock>
    <DemoBlock title={\`\u57FA\u672C\u4F7F\u7528\u7EB5\u5411\`}>
      <Swiper height={150} direction={'vertical'} items={[
        {children: <div style={{height:'100%',width:'100%',background:'red',display:'flex',justifyContent:'center',alignItems:'center'}}>1</div>},
        {children: <div style={{height:'100%',width:'100%',background:'green',display:'flex',justifyContent:'center',alignItems:'center'}}>2</div>},
        {children: <div style={{height:'100%',width:'100%',background:'yellow',display:'flex',justifyContent:'center',alignItems:'center'}}>3</div>},
        {children: <div style={{height:'100%',width:'100%',background:'blue',display:'flex',justifyContent:'center',alignItems:'center'}}>4</div>},
      ]}/>
    </DemoBlock>
    <DemoBlock title={\`\u57FA\u672C\u4F7F\u7528\u7EB5\u5411\`}>
      <Swiper height={150}>
        <Swiper.Item>
          <Image width={'100%'} height={\`100%\`} src={\`https://storage.360buyimg.com/jdc-article/fristfabu.jpg\`} />
        </Swiper.Item>
        <Swiper.Item>
          <Image width={'100%'} height={\`100%\`} src={\`https://storage.360buyimg.com/jdc-article/welcomenutui.jpg\`} />
        </Swiper.Item>
        <Swiper.Item>
          <Image width={'100%'} height={\`100%\`} src={\`https://storage.360buyimg.com/jdc-article/NutUItaro2.jpg\`} />
        </Swiper.Item>
      </Swiper>
    </DemoBlock>
  </>
}
`},10448:function(t,e){e.Z=`import {Swiper as InternalSwiper,SwiperProps} from "./Swiper"
import {SwiperCoreItem} from "./SwiperCore";

type CompoundedComponent = typeof InternalSwiper & {
  Item: typeof SwiperCoreItem
}

const Swiper = InternalSwiper as CompoundedComponent
Swiper.Item = SwiperCoreItem

export type {SwiperProps}
export default Swiper
`},94126:function(t,e){e.Z=`@use "../style/variable" as *;
$swiperCls: 'triones-antm-swiper';

.#{$swiperCls} {
  max-height: 100%;
  display: flex;
  justify-items: center;
  align-items: center;
}
.#{$swiperCls}-core {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  box-sizing: border-box;
  &-box{
    position: relative;
    width: 100%;
    height: 100%;
  }
  &-wrapper{
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    transition-property: transform;
    justify-content: center;
    align-items: center;
    &-horizontal{
      flex-direction: row;
      touch-action: pan-x;
    }
    &-vertical{
      flex-direction: column;
      touch-action: pan-y;
    }
  }
  &-item {
    position: relative;
    flex-shrink: 0;
    display: flex;
    justify-content: center;
    align-items: center;
  }
}

.#{$swiperCls}-taro {
  width: 100%;
  height: 100%;
  &-item{
    display: flex;
    align-items: center;
  }
}
`},51468:function(t,e){e.Z=`import React, { AriaAttributes } from 'react'
import type { CSSProperties, ReactElement } from 'react'
import classNames from 'classnames'

export type NativeProps<S extends string = never> = {
  className?: string
  style?: CSSProperties & Partial<Record<S, string>>
  tabIndex?: number
} & AriaAttributes

export function withNativeProps<P extends NativeProps>(
  props: P,
  element: ReactElement
) {
  const p = {
    ...element.props,
  }
  if (props.className) {
    p.className = classNames(element.props.className, props.className)
  }
  if (props.style) {
    p.style = {
      ...p.style,
      ...props.style,
    }
  }
  if (props.tabIndex !== undefined) {
    p.tabIndex = props.tabIndex
  }
  for (const key in props) {
    if (!props.hasOwnProperty(key)) continue
    if (key.startsWith('data-') || key.startsWith('aria-')) {
      p[key] = props[key]
    }
  }
  return React.cloneElement(element, p)
}

export function attachPropertiesToComponent<C, P extends Record<string, any>>(
  component: C,
  properties: P
): C & P {
  const ret = component as any
  for (const key in properties) {
    if (properties.hasOwnProperty(key)) {
      ret[key] = properties[key]
    }
  }
  return ret
}


export function toCSSLength(val: string | number) {
  return typeof val === 'number' ? \`\${val}px\` : val
}
`},69248:function(t,e){e.Z="export function toCSSLength(val: string | number) {\n  return typeof val === 'number' ? `${val}px` : val;\n}\n"},89755:function(t,e){e.Z=`import { createUpdateEffect, useIsomorphicLayoutEffect } from 'ahooks';

export const useIsomorphicUpdateLayoutEffect = createUpdateEffect(
  useIsomorphicLayoutEffect,
);
`},6494:function(t,e){e.Z=`export function mergeProps<A, B>(a: A, b: B): B & A;
export function mergeProps<A, B, C>(a: A, b: B, c: C): C & B & A;
export function mergeProps<A, B, C, D>(a: A, b: B, c: C, d: D): D & C & B & A;
export function mergeProps(...items: any[]) {
  const ret: any = {};
  items.forEach((item) => {
    if (item) {
      Object.keys(item).forEach((key) => {
        if (item[key] !== undefined) {
          ret[key] = item[key];
        }
      });
    }
  });
  return ret;
}

/**
 * Merge props and return the first non-undefined value.
 * The later has higher priority. e.g. (10, 1, 5) => 5 wins.
 * This is useful with legacy props that have been deprecated.
 */
export function mergeProp<T, DefaultT extends T = T>(
  defaultProp: DefaultT,
  ...propList: T[]
): T | undefined {
  for (let i = propList.length - 1; i >= 0; i -= 1) {
    if (propList[i] !== undefined) {
      return propList[i];
    }
  }
  return defaultProp;
}
`}}]);
